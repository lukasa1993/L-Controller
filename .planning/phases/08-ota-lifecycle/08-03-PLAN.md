---
phase: 08-ota-lifecycle
plan: "03"
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - app/Kconfig
  - app/prj.conf
  - app/src/app/app_config.h
  - app/src/app/bootstrap.c
  - app/src/ota/ota.h
  - app/src/ota/ota.c
  - app/src/persistence/persistence.h
  - app/src/persistence/persistence_types.h
  - app/src/persistence/persistence.c
  - app/src/panel/panel_http.c
  - app/src/panel/panel_status.h
  - app/src/panel/panel_status.c
  - app/src/panel/assets/index.html
  - app/src/panel/assets/main.js
  - README.md
  - scripts/common.sh
  - scripts/validate.sh
autonomous: false
requirements: [OTA-02, OTA-03, OTA-04]
user_setup: []
must_haves:
  truths:
    - "Remote update is fixed to the latest stable GitHub Release for `lukasa1993/L-Controller`, and both the daily automatic check and the explicit `Update now` action stage and apply through the same safety pipeline as local upload."
    - "A new image is never made permanent at stage time; confirmation happens only after the new firmware reaches `APP_READY` and survives the post-boot stable window, otherwise rollback remains possible and is surfaced clearly on the next boot."
    - "Phase 8 approval stays build-first for automation and blocked on a documented browser/curl/device OTA checkpoint that proves local upload, GitHub pull, apply, confirm, rollback, and retry behavior."
  artifacts:
    - path: "app/src/ota/ota.c"
      provides: "GitHub release polling, download-to-stage flow, post-boot confirm logic, and rollback bookkeeping"
      min_lines: 420
      contains: "boot_write_img_confirmed"
    - path: "app/src/panel/panel_http.c"
      provides: "Explicit update-now route and OTA status integration for remote pull control"
      min_lines: 360
      contains: "/api/update"
    - path: "scripts/validate.sh"
      provides: "Phase 8 automated validation entrypoint plus blocking OTA browser/curl/device checklist"
      contains: "Phase 8"
    - path: "scripts/common.sh"
      provides: "Phase 8 curl commands, ready markers, scenario labels, and device checklist helpers"
      contains: "Phase 8"
  key_links:
    - from: "app/src/ota/ota.c"
      to: "app/src/panel/panel_http.c"
      via: "the explicit update-now trigger and the background GitHub worker both converge on the same stage and apply runtime"
      pattern: 'update|github'
    - from: "app/src/ota/ota.c"
      to: "app/src/app/bootstrap.c"
      via: "boot and post-ready uptime drive image confirmation and rollback result visibility instead of confirming at stage time"
      pattern: 'APP_READY|confirm|stable'
    - from: "scripts/validate.sh"
      to: "scripts/common.sh"
      via: "automated output and the blocking Phase 8 live-device checklist remain aligned"
      pattern: 'Phase 8|phase8'
---

<objective>
Implement GitHub remote pull, confirm/rollback handling, and the final validation flow for Phase 8.

Purpose: Finish the OTA lifecycle so the device can fetch the latest stable GitHub release on demand or daily, stage and apply it through the same safety rules as local upload, and only confirm the new image after a healthy post-update boot while keeping rollback truthful and operator-visible.
Output: GitHub Releases remote-update worker, explicit `Update now` action, post-boot confirmation and rollback bookkeeping, and repo docs plus validation flow that prove the complete OTA lifecycle on real hardware.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
@/Users/l/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ota-lifecycle/08-CONTEXT.md
@.planning/phases/08-ota-lifecycle/08-RESEARCH.md
@.planning/phases/08-ota-lifecycle/08-VALIDATION.md
@.planning/phases/08-ota-lifecycle/08-01-PLAN.md
@.planning/phases/08-ota-lifecycle/08-02-PLAN.md
@README.md
@scripts/build.sh
@scripts/common.sh
@scripts/flash.sh
@scripts/console.sh
@scripts/validate.sh
@app/Kconfig
@app/prj.conf
@app/src/app/app_config.h
@app/src/app/bootstrap.c
@app/src/ota/ota.h
@app/src/ota/ota.c
@app/src/persistence/persistence.h
@app/src/persistence/persistence_types.h
@app/src/persistence/persistence.c
@app/src/panel/panel_http.c
@app/src/panel/panel_status.h
@app/src/panel/panel_status.c
@app/src/panel/assets/index.html
@app/src/panel/assets/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the GitHub Releases remote-update worker and explicit update-now trigger</name>
  <files>app/Kconfig, app/prj.conf, app/src/app/app_config.h, app/src/ota/ota.h, app/src/ota/ota.c, app/src/panel/panel_http.c, app/src/panel/assets/main.js</files>
  <action>Implement a repo-owned remote-update flow that is fixed to the public GitHub Releases feed for `lukasa1993/L-Controller`, ignores drafts and prereleases, selects only the latest stable release, and downloads the expected artifact through the same shared OTA stage-and-apply pipeline as local upload. Add one explicit `Update now` action in the authenticated panel/API, and add OTA-owned daily background check work that retries on future cycles after failure or rollback instead of pausing indefinitely. Keep remote failures from blocking local panel access or local upload/apply flows when the device is only upstream-degraded.</action>
  <verify>
    <automated>rg -n "github|releases|latest stable|update now|daily|downloader|http_client|upstream-degraded" app/Kconfig app/prj.conf app/src/app/app_config.h app/src/ota/ota.h app/src/ota/ota.c app/src/panel/panel_http.c app/src/panel/assets/main.js</automated>
  </verify>
  <done>The device can check GitHub Releases on demand or daily and feed the result into the same OTA pipeline used by local upload.</done>
</task>

<task type="auto">
  <name>Task 2: Implement post-boot image confirmation and rollback bookkeeping</name>
  <files>app/src/app/app_config.h, app/src/app/bootstrap.c, app/src/ota/ota.h, app/src/ota/ota.c, app/src/persistence/persistence.h, app/src/persistence/persistence_types.h, app/src/persistence/persistence.c, app/src/panel/panel_status.h, app/src/panel/panel_status.c</files>
  <action>Teach the OTA service to distinguish staged, booting-test-image, confirmed, failed, and rolled-back states across reboot. When booting a test image, do not confirm it immediately. Instead, wait until the firmware reaches the normal `APP_READY` path and survives the short post-boot stable uptime window, then call the MCUboot confirmation API. If the image never confirms and MCUboot reverts, detect that on the next boot and surface rollback reason or last-attempt state clearly in the panel/API. Keep confirmation tied to local health and uptime, not to upstream internet reachability.</action>
  <verify>
    <automated>rg -n "boot_write_img_confirmed|boot_is_img_confirmed|APP_READY|stable|rollback|last_attempt|pending|confirmed" app/src/app/app_config.h app/src/app/bootstrap.c app/src/ota/ota.h app/src/ota/ota.c app/src/persistence/persistence.h app/src/persistence/persistence_types.h app/src/persistence/persistence.c app/src/panel/panel_status.h app/src/panel/panel_status.c</automated>
  </verify>
  <done>The OTA lifecycle now distinguishes successful confirmation from rollback and reports that truthfully across reboot.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs and validation flow for the full OTA lifecycle</name>
  <files>README.md, scripts/common.sh, scripts/validate.sh</files>
  <action>Document the Phase 8 build-first automated path plus the blocking browser, curl, and device OTA checkpoint. Add Phase 8 helper output to `scripts/common.sh`, including signed-artifact expectations, update curl commands, version-rejection scenarios, apply/rollback checkpoints, GitHub update-now flow, daily retry expectations, and ready-state markers. Update `scripts/validate.sh` and `README.md` so a clean checkout can reproduce local upload staging, explicit apply, post-boot confirmation, rollback, and remote GitHub update checks.</action>
  <verify>
    <automated>bash -n scripts/validate.sh scripts/common.sh scripts/build.sh scripts/flash.sh &amp;&amp; rg -n "Phase 8|update|upload|GitHub|rollback|confirm|signed\\.bin|curl" README.md scripts/common.sh scripts/validate.sh scripts/build.sh scripts/flash.sh</automated>
  </verify>
  <done>The repo clearly explains how to build, validate, and approve the full OTA lifecycle on a real device.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm the full OTA lifecycle on a real device</name>
  <files>README.md, scripts/flash.sh, scripts/console.sh, scripts/validate.sh</files>
  <action>Collect final browser, curl, and device confirmation that local upload, GitHub update-now, daily retry behavior, apply, post-boot confirmation, rollback handling, and operator-visible update status all behave correctly on a real device before Phase 8 is considered complete.</action>
  <verify>Human checkpoint approved with local upload staging, same-version and downgrade rejection, explicit apply, post-boot confirmation, rollback visibility, GitHub update-now, daily retry, and panel truthfulness scenarios recorded.</verify>
  <done>Phase 8 OTA behavior is explicitly approved or blocked with scenario-level notes.</done>
  <what-built>Phase 8 full OTA lifecycle with local upload, GitHub remote pull, post-boot confirmation, rollback visibility, and documented validation flow</what-built>
  <how-to-verify>
    Using the built firmware, repo scripts, and one or more browsers on the same LAN, confirm the full OTA lifecycle behaves correctly:
    1. Run `./scripts/validate.sh` and keep the automated sysbuild path green.
    2. Flash the image with `./scripts/flash.sh`.
    3. Open the console with `./scripts/console.sh`, determine the device IP, and confirm the normal ready-state markers appear.
    4. Log in to the local panel and confirm the dedicated update surface shows current version plus any staged or last-attempt state truthfully before beginning.
    5. Upload a newer signed firmware image locally, confirm the device stages it successfully, and confirm no reboot occurs until the explicit apply action is used.
    6. Attempt a same-version upload and one older-version upload, and confirm both are rejected with clear operator feedback and no unsafe staged image remains.
    7. Trigger apply for the newer staged image, confirm the browser disconnects during reboot as expected, and confirm a fresh login is required after the device returns.
    8. After reboot, confirm the new firmware reaches `APP_READY`, remains healthy through the stable window, and is only then marked permanent.
    9. Exercise one rollback path by using an image or flow that fails to confirm, then confirm the device returns to the previous firmware and the next boot shows rollback or failed-attempt status clearly.
    10. Use the explicit `Update now` action while the device has upstream internet access and confirm it checks GitHub Releases, selects the latest stable eligible artifact, and stages or applies it through the same safety rules as local upload.
    11. Force or simulate one failed GitHub update cycle and then allow the next daily check window, confirming the device retries on the next cycle instead of pausing remote updates indefinitely.
    12. Refresh the panel/API before staging, after staging, after apply, and after confirmation or rollback, and confirm current version, staged or last-attempted version, last result, and rollback status remain internally consistent.
    13. Record which local upload staging, same-version rejection, downgrade rejection, explicit apply, post-boot confirmation, rollback visibility, GitHub update-now, daily retry, and panel truthfulness scenarios passed or failed in the Phase 8 summary.
  </how-to-verify>
  <resume-signal>Type "approved" once the Phase 8 OTA scenarios are confirmed, or describe the failing scenario.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/validate.sh
- [ ] rg -n "github|releases|latest stable|update now|daily|downloader|http_client|upstream-degraded" app/Kconfig app/prj.conf app/src/app/app_config.h app/src/ota/ota.h app/src/ota/ota.c app/src/panel/panel_http.c app/src/panel/assets/main.js
- [ ] rg -n "boot_write_img_confirmed|boot_is_img_confirmed|APP_READY|stable|rollback|last_attempt|pending|confirmed" app/src/app/app_config.h app/src/app/bootstrap.c app/src/ota/ota.h app/src/ota/ota.c app/src/persistence/persistence.h app/src/persistence/persistence_types.h app/src/persistence/persistence.c app/src/panel/panel_status.h app/src/panel/panel_status.c
- [ ] bash -n scripts/validate.sh scripts/common.sh scripts/build.sh scripts/flash.sh
- [ ] rg -n "Phase 8|update|upload|GitHub|rollback|confirm|signed\\.bin|curl" README.md scripts/common.sh scripts/validate.sh scripts/build.sh scripts/flash.sh
- [ ] Human checkpoint approved with full OTA-lifecycle scenarios recorded
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Remote GitHub update-now and daily checks both use the same staging and safety pipeline as local upload
- A new image is only marked permanent after healthy post-boot uptime and otherwise remains rollback-capable with truthful result reporting
- Repo docs and validation scripts clearly separate the automated sysbuild gate from the blocking live-device OTA approval flow

</success_criteria>

<output>
After completion, create `.planning/phases/08-ota-lifecycle/08-03-SUMMARY.md`
</output>
