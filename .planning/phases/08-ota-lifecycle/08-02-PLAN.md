---
phase: 08-ota-lifecycle
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/src/ota/ota.h
  - app/src/ota/ota.c
  - app/src/panel/panel_http.c
  - app/src/panel/panel_status.h
  - app/src/panel/panel_status.c
  - app/src/panel/assets/index.html
  - app/src/panel/assets/main.js
autonomous: true
requirements: [OTA-01, OTA-03]
user_setup: []
must_haves:
  truths:
    - "Authenticated local upload stages a newer signed firmware image through the shared OTA pipeline and never reboots immediately; apply remains an explicit separate operator action."
    - "The panel keeps `/api/status` as a compact update summary while dedicated authenticated `/api/update/*` routes carry OTA detail, upload, and mutation behavior."
    - "The update surface shows current version, staged version, last result, and a pending-update warning, and it makes it explicit that applying an update will drop the browser session until the device reboots and the operator logs in again."
  artifacts:
    - path: "app/src/panel/panel_http.c"
      provides: "Authenticated OTA route family with streaming upload and explicit apply semantics"
      min_lines: 320
      contains: "/api/update"
    - path: "app/src/ota/ota.c"
      provides: "Upload-facing stage writer and validation helpers that local panel routes call instead of writing flash directly"
      min_lines: 340
      contains: "upload"
    - path: "app/src/panel/assets/main.js"
      provides: "Dedicated update surface with local upload, staged-image visibility, and apply confirmation UX"
      min_lines: 360
      contains: "update"
  key_links:
    - from: "app/src/panel/panel_http.c"
      to: "app/src/ota/ota.h"
      via: "upload, apply, and stage-clear routes delegate into the OTA service instead of manipulating flash or persistence directly"
      pattern: 'ota_service_'
    - from: "app/src/panel/panel_status.c"
      to: "app/src/ota/ota.h"
      via: "aggregate status renders OTA summary truth from the live service rather than a placeholder string"
      pattern: 'ota'
    - from: "app/src/panel/assets/main.js"
      to: "app/src/panel/panel_http.c"
      via: "frontend uses authenticated exact update routes and refreshes from server truth before and after staging"
      pattern: '/api/update|/api/status'
---

<objective>
Implement authenticated local upload, staged image validation, and the dedicated update surface for Phase 8.

Purpose: Turn the placeholder update card into a real operator workflow for local firmware upload while preserving the locked OTA contract: authenticated only, strictly newer images only, stage first, then apply explicitly, and keep the rest of the panel usable while a staged update is waiting.
Output: Dedicated `/api/update/*` routes, streaming upload into the shared OTA service, truthful OTA status in the panel, and a local update surface that stages first and applies only after explicit confirmation.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ota-lifecycle/08-CONTEXT.md
@.planning/phases/08-ota-lifecycle/08-RESEARCH.md
@.planning/phases/08-ota-lifecycle/08-VALIDATION.md
@.planning/phases/08-ota-lifecycle/08-01-PLAN.md
@app/src/ota/ota.h
@app/src/ota/ota.c
@app/src/panel/panel_http.c
@app/src/panel/panel_status.h
@app/src/panel/panel_status.c
@app/src/panel/assets/index.html
@app/src/panel/assets/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated authenticated OTA routes with binary-safe upload and explicit apply semantics</name>
  <files>app/src/panel/panel_http.c, app/src/ota/ota.h, app/src/ota/ota.c</files>
  <action>Add a dedicated exact OTA route family such as `GET /api/update`, `POST /api/update/upload`, `POST /api/update/apply`, and `POST /api/update/clear` or an equivalently exact set. Keep the current cookie-session trust model, but do not reuse the small JSON request-buffer pattern for firmware upload. The upload handler must stream request chunks into the shared OTA staging pipeline as they arrive, reject same-version and downgrade attempts with operator-meaningful errors, and leave the image only staged until the operator explicitly triggers apply.</action>
  <verify>
    <automated>rg -n "/api/update|upload|apply|clear|application/octet-stream|content-length|ota_service|same-version|downgrade" app/src/panel/panel_http.c app/src/ota/ota.h app/src/ota/ota.c</automated>
  </verify>
  <done>The authenticated HTTP surface can accept a real firmware upload without buffering the entire image in RAM and without violating the staged-then-apply contract.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder update card with a dedicated update workflow</name>
  <files>app/src/panel/assets/index.html, app/src/panel/assets/main.js</files>
  <action>Replace the update placeholder card with a dedicated OTA surface that shows current running version, staged version, last result, and pending-update warning state. Add a local upload flow, keep the rest of the dashboard usable while an image is staged, and use one explicit apply confirmation that warns the operator the panel connection will drop during reboot and that a fresh login will be required after the device returns.</action>
  <verify>
    <automated>rg -n "update|upload|staged|current version|last result|pending|apply|re-login|session" app/src/panel/assets/index.html app/src/panel/assets/main.js</automated>
  </verify>
  <done>The local panel feels like a real OTA staging workflow rather than a read-only placeholder.</done>
</task>

<task type="auto">
  <name>Task 3: Publish truthful OTA summary status and staged-image readiness</name>
  <files>app/src/panel/panel_status.h, app/src/panel/panel_status.c, app/src/ota/ota.h, app/src/ota/ota.c</files>
  <action>Expand the aggregate status payload so the update summary is no longer placeholder-only. Keep the `/api/status` update object compact, but make it truthful: current version, staged version presence, last attempt or result, rollback summary if present, and whether an explicit apply action is currently available. Keep richer upload and progress detail on the dedicated OTA route family rather than inflating the aggregate payload into a second full update API.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "\"update\"|currentVersion|stagedVersion|lastResult|rollback|applyReady|implemented" app/src/panel/panel_status.h app/src/panel/panel_status.c app/src/ota/ota.h app/src/ota/ota.c</automated>
  </verify>
  <done>The dashboard and dedicated update surface now read from live OTA truth instead of hard-coded placeholder copy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "/api/update|upload|apply|clear|application/octet-stream|content-length|ota_service|same-version|downgrade" app/src/panel/panel_http.c app/src/ota/ota.h app/src/ota/ota.c
- [ ] rg -n "update|upload|staged|current version|last result|pending|apply|re-login|session" app/src/panel/assets/index.html app/src/panel/assets/main.js
- [ ] rg -n "\"update\"|currentVersion|stagedVersion|lastResult|rollback|applyReady|implemented" app/src/panel/panel_status.h app/src/panel/panel_status.c app/src/ota/ota.h app/src/ota/ota.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Authenticated local upload stages a newer image through the shared OTA service and never bypasses the staged-then-apply safety contract
- The panel exposes a dedicated update workflow with staged-image visibility and an explicit apply confirmation that warns about reboot and session loss
- `/api/status` becomes truthful for OTA summary while dedicated update routes own detailed OTA interactions

</success_criteria>

<output>
After completion, create `.planning/phases/08-ota-lifecycle/08-02-SUMMARY.md`
</output>
