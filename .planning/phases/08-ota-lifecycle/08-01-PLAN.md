---
phase: 08-ota-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/CMakeLists.txt
  - app/Kconfig
  - app/prj.conf
  - app/sysbuild.conf
  - app/pm_static.yml
  - app/src/app/app_config.h
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/ota/ota.h
  - app/src/ota/ota.c
  - app/src/persistence/persistence.h
  - app/src/persistence/persistence_types.h
  - app/src/persistence/persistence.c
  - scripts/build.sh
  - scripts/flash.sh
autonomous: true
requirements: [OTA-03, OTA-04]
user_setup: []
must_haves:
  truths:
    - "The repo builds and flashes as an MCUboot-backed sysbuild image set with a stable primary, secondary, and settings partition layout rather than a single monolithic app image."
    - "One OTA service owns staged-image writing, image-header version reads, strictly-newer validation, and apply-request plumbing so local upload and remote pull cannot diverge into separate slot-management paths."
    - "Durable OTA state lives inside the existing typed persistence boundary, preserving auth, relay, and schedule ownership instead of introducing ad hoc raw NVS state."
  artifacts:
    - path: "app/pm_static.yml"
      provides: "Static partition-manager layout with MCUboot bootloader, pad, primary, secondary, and settings storage"
      min_lines: 18
      contains: "mcuboot_secondary"
    - path: "app/src/ota/ota.c"
      provides: "Shared OTA runtime with stage-write, version-compare, and apply-request helpers built on Zephyr DFU and MCUboot APIs"
      min_lines: 260
      contains: "boot_request_upgrade"
    - path: "scripts/build.sh"
      provides: "Sysbuild-aware developer build flow that produces MCUboot OTA artifacts"
      contains: "--sysbuild"
    - path: "app/src/persistence/persistence.c"
      provides: "Typed durable OTA state save and load plumbing"
      contains: "PERSISTENCE_NVS_ID_OTA"
  key_links:
    - from: "scripts/build.sh"
      to: "app/sysbuild.conf"
      via: "the repo build flow must invoke the same sysbuild shape that enables MCUboot for OTA-capable firmware artifacts"
      pattern: 'sysbuild|BOOTLOADER_MCUBOOT'
    - from: "app/src/app/bootstrap.c"
      to: "app/src/ota/ota.h"
      via: "boot composes the OTA service beside the other subsystem owners before panel and later update surfaces depend on live OTA truth"
      pattern: 'ota_service_'
    - from: "app/src/ota/ota.c"
      to: "app/src/persistence/persistence.c"
      via: "staged version and attempt metadata persist through the same typed storage boundary as the rest of the firmware state"
      pattern: 'PERSISTENCE_NVS_ID_OTA|ota'
---

<objective>
Enable MCUboot/sysbuild, partitioning, and shared staged-image plumbing for Phase 8.

Purpose: Turn the project from a single-image firmware build into a real bootloader-backed OTA foundation, then add one repo-owned OTA runtime that later plans can call for local upload and GitHub pull without duplicating slot, version, or persistence logic.
Output: A sysbuild + MCUboot firmware layout, an initialized OTA service in app boot, typed durable OTA state, and one shared staging/apply pipeline ready for local and remote update flows.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ota-lifecycle/08-CONTEXT.md
@.planning/phases/08-ota-lifecycle/08-RESEARCH.md
@.planning/phases/08-ota-lifecycle/08-VALIDATION.md
@scripts/build.sh
@scripts/flash.sh
@app/CMakeLists.txt
@app/Kconfig
@app/prj.conf
@app/sysbuild.conf
@app/pm_static.yml
@app/src/app/app_config.h
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/ota/ota.h
@app/src/persistence/persistence.h
@app/src/persistence/persistence_types.h
@app/src/persistence/persistence.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the repo to an MCUboot sysbuild with a stable dual-slot partition map</name>
  <files>app/sysbuild.conf, app/prj.conf, app/pm_static.yml, scripts/build.sh, scripts/flash.sh</files>
  <action>Enable MCUboot through sysbuild, reshape the static partition map so the board has an explicit bootloader, pad, primary slot, secondary slot, and settings storage, and update the build or flash scripts so normal repo usage now builds and flashes the sysbuild image set instead of a single raw app image. Keep the current secrets overlay workflow intact and make the new build output produce the signed firmware artifacts that later upload and remote-pull paths will consume.</action>
  <verify>
    <automated>rg -n "BOOTLOADER_MCUBOOT|mcuboot|secondary|pad|settings_storage|sysbuild" app/sysbuild.conf app/prj.conf app/pm_static.yml scripts/build.sh scripts/flash.sh</automated>
  </verify>
  <done>The project has a real bootloader-backed image layout and a developer workflow that matches it.</done>
</task>

<task type="auto">
  <name>Task 2: Add the OTA service boundary, config surface, and typed durable OTA state</name>
  <files>app/CMakeLists.txt, app/Kconfig, app/prj.conf, app/src/app/app_config.h, app/src/app/app_context.h, app/src/app/bootstrap.c, app/src/ota/ota.h, app/src/ota/ota.c, app/src/persistence/persistence.h, app/src/persistence/persistence_types.h, app/src/persistence/persistence.c</files>
  <action>Add a real `ota_service` implementation to the build, wire it into `app_context` and `app_boot()`, and define only the minimal config and persistence surface Phase 8 needs now: current version truth, staged-image metadata, last attempt or result bookkeeping, and remote-policy placeholders that later plans can fill in. Keep OTA state inside the existing typed persistence boundary rather than inventing raw flash or raw NVS ownership outside it, and avoid resetting auth, relay, or schedule data just because OTA becomes available.</action>
  <verify>
    <automated>test -f app/src/ota/ota.c &amp;&amp; rg -n "ota_service|struct ota_|staged|last_attempt|rollback|PERSISTENCE_NVS_ID_OTA|update" app/CMakeLists.txt app/Kconfig app/prj.conf app/src/app/app_config.h app/src/app/app_context.h app/src/app/bootstrap.c app/src/ota/ota.h app/src/ota/ota.c app/src/persistence/persistence.h app/src/persistence/persistence_types.h app/src/persistence/persistence.c</automated>
  </verify>
  <done>The firmware now has one live OTA owner and durable OTA state instead of a placeholder header.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the shared stage-write, version-compare, and apply-request plumbing</name>
  <files>app/src/ota/ota.h, app/src/ota/ota.c, app/src/app/bootstrap.c, app/src/persistence/persistence.c</files>
  <action>Build the OTA foundation around Zephyr `flash_img` and MCUboot APIs so later callers can stream bytes into the secondary slot, inspect current and staged image headers, enforce the locked \"strictly newer only\" rule, and request a test upgrade without marking the image permanent at stage time. Make this one shared pipeline the only way to move from a candidate firmware image to a staged or ready-to-apply update state.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "flash_img|boot_read_bank_header|boot_request_upgrade|boot_is_img_confirmed|strictly newer|same-version|downgrade|staged" app/src/ota/ota.h app/src/ota/ota.c app/src/app/bootstrap.c app/src/persistence/persistence.c</automated>
  </verify>
  <done>The repo has one OTA staging and apply foundation that both future update paths must reuse.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "BOOTLOADER_MCUBOOT|mcuboot|secondary|pad|settings_storage|sysbuild" app/sysbuild.conf app/prj.conf app/pm_static.yml scripts/build.sh scripts/flash.sh
- [ ] test -f app/src/ota/ota.c
- [ ] rg -n "ota_service|struct ota_|staged|last_attempt|rollback|PERSISTENCE_NVS_ID_OTA|update" app/CMakeLists.txt app/Kconfig app/prj.conf app/src/app/app_config.h app/src/app/app_context.h app/src/app/bootstrap.c app/src/ota/ota.h app/src/ota/ota.c app/src/persistence/persistence.h app/src/persistence/persistence_types.h app/src/persistence/persistence.c
- [ ] rg -n "flash_img|boot_read_bank_header|boot_request_upgrade|boot_is_img_confirmed|same-version|downgrade|staged" app/src/ota/ota.h app/src/ota/ota.c app/src/app/bootstrap.c app/src/persistence/persistence.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo builds as an MCUboot-backed sysbuild image set with a stable dual-slot partition map
- One OTA service owns staged-image writing, version comparison, and apply-request plumbing instead of leaving future routes to manipulate flash or bootloader state directly
- Durable OTA metadata lives inside the existing typed persistence boundary without disturbing prior auth, relay, or schedule ownership

</success_criteria>

<output>
After completion, create `.planning/phases/08-ota-lifecycle/08-01-SUMMARY.md`
</output>
