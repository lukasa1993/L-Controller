---
phase: 07-scheduling
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/actions/actions.h
  - app/src/actions/actions.c
  - app/src/panel/panel_status.h
  - app/src/panel/panel_status.c
  - app/src/persistence/persistence.c
  - app/src/recovery/recovery.h
  - app/src/recovery/recovery.c
  - app/src/scheduler/scheduler.h
  - app/src/scheduler/scheduler.c
autonomous: true
requirements: [SCHED-03, SCHED-04]
user_setup: []
must_haves:
  truths:
    - "Initial trusted-time acquisition is explicit: the scheduler attempts sync, requests one recovery reset on first failure, and then stays degraded/inactive on the next boot instead of reboot-looping or automating blindly."
    - "The scheduler evaluates only trusted UTC minute boundaries, never backfills missed jobs, and recomputes future runs after reboot or major correction events."
    - "Manual and scheduled commands share one serialized action path, while aggregate scheduler status surfaces clock trust, automation active, next run, last result, and recent problems from one runtime snapshot."
  artifacts:
    - path: "app/src/scheduler/scheduler.c"
      provides: "Trusted-minute evaluation loop, compiled schedule runtime, recent-problem ring, and dispatcher-backed execution"
      min_lines: 320
      contains: "ACTION_DISPATCH_SOURCE_SCHEDULER"
    - path: "app/src/panel/panel_status.c"
      provides: "Aggregate scheduler status payload with next run, last result, and clock-trust visibility"
      min_lines: 170
      contains: "scheduler"
    - path: "app/src/actions/actions.c"
      provides: "Stable scheduled execution seam that the scheduler can call without duplicating relay or persistence logic"
      contains: "ACTION_DISPATCH_SOURCE_SCHEDULER"
  key_links:
    - from: "app/src/scheduler/scheduler.c"
      to: "app/src/actions/actions.h"
      via: "scheduled jobs route through the action dispatcher instead of manipulating relay state directly"
      pattern: 'ACTION_DISPATCH_SOURCE_SCHEDULER|action_dispatcher_execute'
    - from: "app/src/panel/panel_status.c"
      to: "app/src/scheduler/scheduler.h"
      via: "aggregate status renders scheduler runtime truth rather than placeholder persistence counts"
      pattern: 'scheduler'
    - from: "app/src/app/bootstrap.c"
      to: "app/src/scheduler/scheduler.c"
      via: "boot logs and runtime composition surface scheduler readiness and degraded clock state from the live service"
      pattern: 'scheduler_service_'
---

<objective>
Implement the live scheduler engine, trusted-minute evaluation, and truthful runtime status for Phase 7.

Purpose: Turn the Phase 7 contract from Plan 1 into a real runtime that loads durable schedules, computes the next future run only when time is trustworthy, dispatches jobs through the action engine, and exposes the operator-visible scheduler truth required for management and validation work.
Output: A live scheduler runtime with trusted-minute evaluation, dispatcher-backed scheduled execution, a bounded recent-problem history, and aggregate status fields ready for the dedicated scheduling surface in Plan 3.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-scheduling/07-CONTEXT.md
@.planning/phases/07-scheduling/07-RESEARCH.md
@.planning/phases/07-scheduling/07-VALIDATION.md
@.planning/phases/07-scheduling/07-01-PLAN.md
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/actions/actions.h
@app/src/actions/actions.c
@app/src/panel/panel_status.h
@app/src/panel/panel_status.c
@app/src/persistence/persistence.c
@app/src/recovery/recovery.h
@app/src/recovery/recovery.c
@app/src/relay/relay.h
@app/src/scheduler/scheduler.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire trusted UTC time and implement the one-reset then degrade recovery path</name>
  <files>app/src/scheduler/scheduler.h, app/src/scheduler/scheduler.c, app/src/app/bootstrap.c, app/src/recovery/recovery.h, app/src/recovery/recovery.c</files>
  <action>Implement a scheduler-owned trusted-clock helper that acquires UTC time using Zephyr-native clock primitives, updates `CLOCK_REALTIME`, and tracks whether time is trustworthy for automation. If initial trusted-time acquisition fails on a normal boot, request one dedicated clock-specific recovery reset. If the next boot sees the retained clock-failure trigger again, bring the panel up normally but leave scheduling inactive and visibly degraded instead of entering a reset loop.</action>
  <verify>
    <automated>rg -n "SNTP|CLOCK_REALTIME|clock|trusted|degraded|RECOVERY|reset trigger|UTC" app/src/scheduler/scheduler.h app/src/scheduler/scheduler.c app/src/app/bootstrap.c app/src/recovery/recovery.h app/src/recovery/recovery.c</automated>
  </verify>
  <done>The firmware has a real trusted-time acquisition path and the locked “restart, then degrade” behavior is implemented through existing recovery ownership rather than ad hoc reboot calls.</done>
</task>

<task type="auto">
  <name>Task 2: Implement trusted-minute schedule evaluation and next-run bookkeeping</name>
  <files>app/src/scheduler/scheduler.h, app/src/scheduler/scheduler.c, app/src/app/app_context.h, app/src/app/bootstrap.c</files>
  <action>Turn the scheduler into a live runtime that loads or compiles saved schedules, tracks the next due UTC minute for each enabled entry, and responds explicitly to trust acquisition, trust loss, backward jumps, and normalized-minute correction events. Keep the execution model conservative: evaluate only trusted minute boundaries, baseline the current minute when trust changes materially, skip missed jobs, and record clock or schedule problems into a bounded 10-entry recent-problem ring owned by the scheduler.</action>
  <verify>
    <automated>test -f app/src/scheduler/scheduler.c &amp;&amp; rg -n "next_run|evaluate|minute|recompute|clock|history|problem|UTC|normalized" app/src/scheduler/scheduler.c app/src/scheduler/scheduler.h app/src/app/app_context.h app/src/app/bootstrap.c</automated>
  </verify>
  <done>The firmware owns a real scheduler loop that knows when automation is safe, which schedule is next, and why jobs were skipped, deferred, or recomputed.</done>
</task>

<task type="auto">
  <name>Task 3: Dispatch scheduled jobs through the existing action engine with serialization and no-op protection</name>
  <files>app/src/scheduler/scheduler.c, app/src/actions/actions.h, app/src/actions/actions.c</files>
  <action>Route every due schedule through `action_dispatcher_execute(..., ACTION_DISPATCH_SOURCE_SCHEDULER, ...)`, record success, rejection, or skip outcomes in scheduler runtime state, and preserve the existing relay and persistence semantics from Phase 6. Serialize manual and scheduled action execution so they cannot interleave unpredictably, and add a no-op fast path for desired-state repeats so frequently recurring schedules do not cause avoidable NVS writes when relay desired state is already correct.</action>
  <verify>
    <automated>rg -n "ACTION_DISPATCH_SOURCE_SCHEDULER|action_dispatcher_execute|last_result|scheduled|mutex|lock|desired" app/src/scheduler/scheduler.c app/src/actions/actions.c app/src/actions/actions.h &amp;&amp; ! rg -n "gpio_pin_(set|configure)" app/src/scheduler/scheduler.c</automated>
  </verify>
  <done>Scheduled jobs use the same execution seam as manual commands without creating a second actuation path, race window, or avoidable write-amplification path.</done>
</task>

<task type="auto">
  <name>Task 4: Publish truthful scheduler runtime status and degraded clock visibility</name>
  <files>app/src/panel/panel_status.h, app/src/panel/panel_status.c, app/src/app/bootstrap.c, app/src/scheduler/scheduler.h, app/src/scheduler/scheduler.c</files>
  <action>Expand the aggregate status surface so scheduler data is no longer placeholder-only. Surface implemented, clock trusted, automation active, schedule counts, next run, last result, and recent-problem visibility from the live scheduler snapshot. Update boot logging so trust changes, failed initial sync with reset-then-degrade behavior, and invalid schedule resets are visible before the panel is opened.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "clockTrusted|automationActive|nextRun|lastResult|problem|implemented|scheduler|degraded|UTC" app/src/panel/panel_status.h app/src/panel/panel_status.c app/src/app/bootstrap.c app/src/scheduler/scheduler.h app/src/scheduler/scheduler.c</automated>
  </verify>
  <done>The operator and later UI work can now rely on truthful scheduler runtime status instead of placeholder counts and fixed copy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "SNTP|CLOCK_REALTIME|clock|trusted|degraded|RECOVERY|reset trigger|UTC" app/src/scheduler/scheduler.h app/src/scheduler/scheduler.c app/src/app/bootstrap.c app/src/recovery/recovery.h app/src/recovery/recovery.c
- [ ] test -f app/src/scheduler/scheduler.c &amp;&amp; rg -n "next_run|evaluate|minute|recompute|clock|history|problem|UTC|normalized" app/src/scheduler/scheduler.c app/src/scheduler/scheduler.h app/src/app/app_context.h app/src/app/bootstrap.c
- [ ] rg -n "ACTION_DISPATCH_SOURCE_SCHEDULER|action_dispatcher_execute|last_result|scheduled|mutex|lock|desired" app/src/scheduler/scheduler.c app/src/actions/actions.c app/src/actions/actions.h
- [ ] ! rg -n "gpio_pin_(set|configure)" app/src/scheduler/scheduler.c
- [ ] rg -n "clockTrusted|automationActive|nextRun|lastResult|problem|implemented|scheduler|degraded|UTC" app/src/panel/panel_status.h app/src/panel/panel_status.c app/src/app/bootstrap.c app/src/scheduler/scheduler.h app/src/scheduler/scheduler.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a live scheduler runtime that acquires trusted UTC time, performs one reset-then-degrade fallback on initial sync failure, and never replays missed jobs implicitly
- Scheduled and manual commands reuse one serialized Phase 6 action path instead of creating a second relay actuation path or race window
- Aggregate scheduler status becomes truthful enough for operator controls and manual verification to build on

</success_criteria>

<output>
After completion, create `.planning/phases/07-scheduling/07-02-SUMMARY.md`
</output>
