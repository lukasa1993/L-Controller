---
phase: 07-scheduling
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/CMakeLists.txt
  - app/Kconfig
  - app/prj.conf
  - app/src/app/app_config.h
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/persistence/persistence.h
  - app/src/persistence/persistence_types.h
  - app/src/persistence/persistence.c
  - app/src/scheduler/scheduler.h
  - app/src/scheduler/scheduler.c
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-04]
user_setup: []
must_haves:
  truths:
    - "The firmware has an explicit scheduler clock-trust model, and schedule execution stays inactive until time is trustworthy rather than guessing from uptime or boot readiness."
    - "Saved schedules use a canonical UTC 5-field cron contract with numeric minute/hour/day-of-month/month/day-of-week grammar, standard day-of-month/day-of-week OR semantics, and only opposite-state same-minute overlaps rejected before persistence."
    - "Boot and resynchronization behavior are conservative: when trust is first acquired or materially corrected, the scheduler baselines the current minute, skips missed jobs, and only computes future runs."
  artifacts:
    - path: "app/src/scheduler/scheduler.c"
      provides: "Scheduler runtime boundary with clock-trust state, minute baseline logic, and cron-parse or evaluate helpers"
      min_lines: 220
      contains: "scheduler_service_"
    - path: "app/src/persistence/persistence_types.h"
      provides: "Full cron-based persisted schedule contract that replaces the legacy weekday-plus-minute shape"
      contains: "cron"
    - path: "app/src/persistence/persistence.c"
      provides: "Layout-bump-backed schedule validation and conflict rejection before durable save"
      contains: "schedule"
  key_links:
    - from: "app/src/app/bootstrap.c"
      to: "app/src/scheduler/scheduler.h"
      via: "boot composes the scheduler runtime after persistence is ready and before later status surfaces rely on scheduler truth"
      pattern: 'scheduler_service_'
    - from: "app/src/persistence/persistence.c"
      to: "app/src/scheduler/scheduler.c"
      via: "schedule save or load logic reuses the scheduler cron validation and conflict rules instead of inventing a second parser"
      pattern: 'cron|schedule'
    - from: "app/src/scheduler/scheduler.c"
      to: "app/src/persistence/persistence_types.h"
      via: "runtime schedule entries are compiled from the canonical persisted cron contract"
      pattern: 'cron|schedule'
---

<objective>
Define the trusted clock model, durable cron schema, and missed-job semantics for Phase 7.

Purpose: Turn the placeholder scheduler boundary and the legacy weekly schedule record into a concrete Phase 7 foundation that gives execution a real trusted-time contract, a layout-bump-backed persistence shape, and conservative reboot and resynchronization rules.
Output: A buildable scheduler service boundary, canonical 5-field cron persistence contract, explicit conflict validation, and boot-owned trusted-time gating ready for the live runtime work in Plan 2.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-scheduling/07-CONTEXT.md
@.planning/phases/07-scheduling/07-RESEARCH.md
@.planning/phases/07-scheduling/07-VALIDATION.md
@app/CMakeLists.txt
@app/Kconfig
@app/prj.conf
@app/src/app/app_config.h
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/persistence/persistence.h
@app/src/persistence/persistence_types.h
@app/src/persistence/persistence.c
@app/src/actions/actions.h
@app/src/scheduler/scheduler.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the scheduler build surface and explicit trusted-clock contract</name>
  <files>app/CMakeLists.txt, app/Kconfig, app/prj.conf, app/src/app/app_config.h, app/src/scheduler/scheduler.h, app/src/scheduler/scheduler.c</files>
  <action>Register a real `scheduler.c` implementation, add only the minimal config surface this phase needs for scheduler cadence and trusted-time ownership, and define runtime types for clock-trust state, degraded reason, automation active, next-run, and last-result visibility. Lock Phase 7 time semantics here: scheduler matching is minute-resolution only, every authored or rendered schedule is explicitly UTC, and the phase does not add timezone or DST configuration.</action>
  <verify>
    <automated>rg -n "scheduler\.c|scheduler_service|clock|trusted|degraded|next_run|automation|UTC" app/CMakeLists.txt app/Kconfig app/prj.conf app/src/app/app_config.h app/src/scheduler/scheduler.h app/src/scheduler/scheduler.c</automated>
  </verify>
  <done>The repo has a real scheduler subsystem boundary with an explicit trusted-time contract instead of a stub header and placeholder assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate persisted schedules to a canonical cron contract with conservative validation</name>
  <files>app/src/persistence/persistence.h, app/src/persistence/persistence_types.h, app/src/persistence/persistence.c, app/src/scheduler/scheduler.h, app/src/scheduler/scheduler.c</files>
  <action>Replace the legacy `days_of_week_mask` plus `minute_of_day` contract with a canonical UTC full 5-field cron representation that still keeps `schedule_id`, `action_id`, and `enabled`. Take a one-time `APP_PERSISTENCE_LAYOUT_VERSION` bump for Phase 7 rather than building a broader per-section migration framework. Lock the supported grammar here: numeric `minute hour day-of-month month day-of-week` with `*`, lists, ranges, and step values; standard day-of-month/day-of-week OR semantics; and no seconds field or macro shortcuts. Reuse the Phase 4 persistence validation posture: validate syntax, duplicate IDs, referenced actions, maximum counts, allow same-state overlaps, and reject opposite-state same-minute relay commands before any save lands.</action>
  <verify>
    <automated>rg -n "cron|expression|schedule_id|action_id|enabled|conflict|UTC|day_of_month|day_of_week|layout|relay-on|relay-off" app/src/persistence/persistence.h app/src/persistence/persistence_types.h app/src/persistence/persistence.c app/src/scheduler/scheduler.c</automated>
  </verify>
  <done>The durable schedule shape is Phase 7-ready, explicitly UTC, backed by a one-time layout bump, and incapable of persisting ambiguous or conflicting schedule state silently.</done>
</task>

<task type="auto">
  <name>Task 3: Compose scheduler ownership into boot with skip-missed and future-only semantics</name>
  <files>app/src/app/app_context.h, app/src/app/bootstrap.c, app/src/scheduler/scheduler.h, app/src/scheduler/scheduler.c</files>
  <action>Add `scheduler_service` to `app_context`, initialize it only after persistence, actions, and recovery context are available, and make boot semantics conservative. The scheduler must stay inactive until time becomes trustworthy; when trust is first acquired or when time moves backward or changes the normalized current UTC minute, it must baseline the corrected minute, skip missed jobs, and compute only future runs. Record enough degraded or problem state for later status and UI work, but do not add schedule CRUD or operator controls in this plan.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "scheduler_service|trusted|baseline|skip missed|future run|problem|automation|UTC|backward|normalized" app/src/app/app_context.h app/src/app/bootstrap.c app/src/scheduler/scheduler.h app/src/scheduler/scheduler.c</automated>
  </verify>
  <done>Boot now owns a real scheduler foundation with explicit trusted-time and missed-job semantics instead of leaving those rules implicit for later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "scheduler\.c|scheduler_service|clock|trusted|degraded|next_run|automation" app/CMakeLists.txt app/Kconfig app/prj.conf app/src/app/app_config.h app/src/scheduler/scheduler.h app/src/scheduler/scheduler.c
- [ ] rg -n "cron|expression|schedule_id|action_id|enabled|conflict|legacy|migration" app/src/persistence/persistence.h app/src/persistence/persistence_types.h app/src/persistence/persistence.c app/src/scheduler/scheduler.c
- [ ] rg -n "scheduler_service|trusted|baseline|skip missed|future run|problem|automation" app/src/app/app_context.h app/src/app/bootstrap.c app/src/scheduler/scheduler.h app/src/scheduler/scheduler.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a real scheduler service and trusted-time contract rather than a stub-only placeholder
- Saved schedules use an explicitly UTC canonical cron contract, a one-time layout bump, standard day-of-month/day-of-week OR semantics, and opposite-state conflict rejection before persistence
- Boot semantics explicitly state that untrusted or corrected time does not replay missed jobs, only schedules future runs, and treats any normalized-minute change or backward jump as a correction event

</success_criteria>

<output>
After completion, create `.planning/phases/07-scheduling/07-01-SUMMARY.md`
</output>
