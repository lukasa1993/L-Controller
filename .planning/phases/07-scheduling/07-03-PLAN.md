---
phase: 07-scheduling
plan: "03"
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - app/src/actions/actions.h
  - app/src/actions/actions.c
  - app/src/panel/panel_http.c
  - app/src/panel/panel_status.h
  - app/src/panel/panel_status.c
  - app/src/panel/assets/index.html
  - app/src/panel/assets/main.js
  - README.md
  - scripts/common.sh
  - scripts/validate.sh
autonomous: false
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04]
user_setup: []
must_haves:
  truths:
    - "Authenticated schedule management lets the operator create, edit, enable, disable, and delete cron schedules without exposing raw internal action IDs in the normal UI flow."
    - "The dedicated scheduling surface shows clock trust, automation active, next run, last result, and recent problems together, while manual relay control semantics from Phase 6 remain unchanged."
    - "Phase 7 approval stays build-first for automation and blocked on a documented browser/curl/device sign-off that proves schedule management, trusted-time gating, reboot skip semantics, and time-correction behavior."
  artifacts:
    - path: "app/src/panel/panel_http.c"
      provides: "Authenticated exact schedule-management routes with operator-facing validation feedback"
      min_lines: 260
      contains: "/api/schedules"
    - path: "app/src/panel/assets/main.js"
      provides: "Dedicated scheduler management UI with create or edit flows, next-run visibility, and degraded clock feedback"
      min_lines: 320
      contains: "scheduler"
    - path: "scripts/validate.sh"
      provides: "Phase 7 automated validation entrypoint plus blocking live-device scheduler checklist"
      contains: "Phase 7"
  key_links:
    - from: "app/src/panel/assets/main.js"
      to: "app/src/panel/panel_http.c"
      via: "frontend uses authenticated exact schedule routes and refreshes from server truth after each schedule mutation"
      pattern: '/api/schedules|/api/status'
    - from: "app/src/panel/panel_http.c"
      to: "app/src/actions/actions.h"
      via: "schedule creation and editing use operator-friendly action choices that still resolve to the stable Phase 6 action IDs"
      pattern: 'action'
    - from: "scripts/validate.sh"
      to: "scripts/common.sh"
      via: "automated output and the blocking Phase 7 browser or curl or device checkpoint remain aligned"
      pattern: 'Phase 7|phase7'
---

<objective>
Add authenticated schedule management, the dedicated scheduler UI, and the final validation flow for Phase 7.

Purpose: Turn the live scheduler runtime into a first-class operator workflow that supports full schedule lifecycle management from the local panel and exact HTTP routes, then document and block approval on the real-device behaviors that build checks alone cannot prove.
Output: Exact authenticated schedule CRUD routes, a dedicated scheduling operator surface, updated repo validation and docs, and a blocking manual checkpoint that proves trusted-time, reboot, conflict, and execution behavior.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
@/Users/l/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-scheduling/07-CONTEXT.md
@.planning/phases/07-scheduling/07-RESEARCH.md
@.planning/phases/07-scheduling/07-VALIDATION.md
@.planning/phases/07-scheduling/07-01-PLAN.md
@.planning/phases/07-scheduling/07-02-PLAN.md
@README.md
@scripts/build.sh
@scripts/common.sh
@scripts/flash.sh
@scripts/console.sh
@scripts/validate.sh
@app/src/actions/actions.h
@app/src/actions/actions.c
@app/src/panel/panel_http.c
@app/src/panel/panel_status.h
@app/src/panel/panel_status.c
@app/src/panel/assets/index.html
@app/src/panel/assets/main.js
@app/src/scheduler/scheduler.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exact authenticated schedule management routes and operator-facing action choices</name>
  <files>app/src/actions/actions.h, app/src/actions/actions.c, app/src/panel/panel_http.c, app/src/panel/panel_status.h, app/src/panel/panel_status.c</files>
  <action>Add exact authenticated schedule-management routes such as `GET /api/schedules`, `POST /api/schedules/create`, `POST /api/schedules/update`, and `POST /api/schedules/delete`, or an equivalently exact route set that avoids wildcard-heavy parsing. Return schedule snapshots, operator-facing validation failures, and friendly action choices so the panel can work without exposing raw internal action IDs in its normal UI. Keep the existing cookie-session model, keep handlers thin, and keep relay actuation or scheduler semantics delegated to existing runtime owners.</action>
  <verify>
    <automated>rg -n "/api/schedules|schedule|cron|conflict|friendly|action" app/src/panel/panel_http.c app/src/panel/panel_status.c app/src/panel/panel_status.h app/src/actions/actions.c app/src/actions/actions.h</automated>
  </verify>
  <done>The authenticated HTTP surface can manage schedules end to end without exposing internal action plumbing or duplicating scheduler business logic inside the route layer.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder card with a dedicated scheduler management surface</name>
  <files>app/src/panel/assets/index.html, app/src/panel/assets/main.js</files>
  <action>Replace the placeholder scheduler card with a dedicated scheduling operator surface that shows clock trust, automation active, next run, last result, recent scheduler problems, and the full schedule list. Support create, edit, enable, disable, and delete flows with full 5-field cron input, clear validation feedback, and friendly action naming. Keep the rest of the dashboard coherent, keep manual relay control semantics unchanged, and avoid surfacing raw `action_id` values in the normal UI.</action>
  <verify>
    <automated>rg -n "scheduler|cron|next run|last result|clock|history|create|edit|disable|delete" app/src/panel/assets/index.html app/src/panel/assets/main.js &amp;&amp; ! rg -n "action_id" app/src/panel/assets/index.html app/src/panel/assets/main.js</automated>
  </verify>
  <done>The local panel feels like a serious scheduler workflow rather than a placeholder card or hidden API-only feature.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs and validation flow for Phase 7 schedule management and trusted-time approval</name>
  <files>README.md, scripts/common.sh, scripts/validate.sh</files>
  <action>Document the Phase 7 build-first automated path plus the blocking browser, curl, and device schedule-validation checkpoint. Add Phase 7 helper output to `scripts/common.sh`, including example schedule-management curls, trusted-time checkpoints, and scenario labels. Update `scripts/validate.sh` and `README.md` so a clean checkout can reproduce schedule creation, edit, disable, delete, conflict rejection, trusted-time gating, reboot skip semantics, and major clock-correction behavior.</action>
  <verify>
    <automated>bash -n scripts/validate.sh scripts/common.sh scripts/build.sh &amp;&amp; rg -n "Phase 7|schedule|cron|clock|missed|resync|curl" README.md scripts/common.sh scripts/validate.sh</automated>
  </verify>
  <done>The repo clearly explains how to build, validate, and approve Phase 7 scheduler behavior on a real device.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm live schedule management and runtime behavior on a real device</name>
  <files>README.md, scripts/flash.sh, scripts/console.sh, scripts/validate.sh</files>
  <action>Collect final browser, curl, and device confirmation that schedule lifecycle management, trusted-time gating, the one-reset then degrade path, reboot semantics, manual-versus-scheduled interaction, conflict handling, and scheduled relay execution are correct on a real device before Phase 7 is considered complete.</action>
  <verify>Human checkpoint approved with scheduler-untrusted gating, initial-sync reset-then-degrade, schedule create, edit, enable, disable, delete, manual-versus-scheduled interaction, scheduled execution, reboot persistence with list re-check, skipped-missed-job behavior, major clock-correction recompute behavior, conflict rejection, and recent-problem visibility recorded.</verify>
  <done>Phase 7 scheduling behavior is explicitly approved or blocked with scenario-level notes.</done>
  <what-built>Phase 7 schedule management UI, exact authenticated schedule CRUD routes, truthful scheduler status, and documented trusted-time validation flow</what-built>
  <how-to-verify>
    Using the built firmware, repo scripts, and one or more browsers on the same LAN, confirm Phase 7 scheduling behaves correctly:
    1. Run `./scripts/validate.sh` and keep the automated build path green.
    2. Flash the image with `./scripts/flash.sh`.
    3. Open the console with `./scripts/console.sh`, determine the device IP from the boot log, and confirm the scheduler reports untrusted or syncing state before trusted time is available.
    4. Deliberately exercise one failed initial time-sync path and confirm the device performs one recovery reset, then comes back with the panel usable while scheduling stays inactive and visibly degraded instead of reboot-looping.
    5. Log in through the local panel and confirm the scheduler view renders clock-trust, automation-active, next-run, last-result, and recent-problem status without exposing raw internal action IDs.
    6. Use the panel or the documented curl flow to create a relay schedule a few minutes ahead and confirm the next run appears immediately.
    7. Edit that schedule, then disable and re-enable it, and confirm each mutation is reflected in the list and next-run display.
    8. Delete one schedule and confirm it disappears cleanly without disturbing unrelated schedules.
    9. Create a near-future relay-on or relay-off schedule, issue one manual relay command before the due minute, and confirm the next scheduled run still executes normally instead of being disarmed or paused.
    10. Wait for the due minute and confirm the physical relay, `GET /api/status`, and scheduler last-result or next-run fields all agree that execution came from the scheduler path.
    11. Reboot the device before one scheduled minute passes, then reopen the schedule view or call `GET /api/schedules` after boot and confirm edited, disabled, enabled, and deleted schedule state is still correct.
    12. Restore trusted time after that reboot and confirm the missed run is skipped rather than replayed automatically.
    13. Force or simulate a major clock correction and confirm the scheduler recomputes only future runs without backfilling earlier missed minutes.
    14. Attempt to save two enabled schedules that would command opposite relay states in the same minute and confirm the save is rejected with operator-visible feedback.
    15. Confirm recent scheduler problems remain visible in the panel or status payload after rejected or degraded-time scenarios.
    16. Record which untrusted gating, initial-sync reset-then-degrade, create, edit, enable, disable, delete, manual-versus-scheduled interaction, scheduled execution, reboot persistence with list re-check, missed-job skip, time-correction recompute, conflict rejection, and recent-problem visibility scenarios passed or failed in the Phase 7 summary.
  </how-to-verify>
  <resume-signal>Type "approved" once the Phase 7 scheduling scenarios are confirmed, or describe the failing scenario.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/validate.sh
- [ ] rg -n "/api/schedules|schedule|cron|conflict|friendly|action" app/src/panel/panel_http.c app/src/panel/panel_status.c app/src/panel/panel_status.h app/src/actions/actions.c app/src/actions/actions.h
- [ ] rg -n "scheduler|cron|next run|last result|clock|history|create|edit|disable|delete" app/src/panel/assets/index.html app/src/panel/assets/main.js
- [ ] ! rg -n "action_id" app/src/panel/assets/index.html app/src/panel/assets/main.js
- [ ] bash -n scripts/validate.sh scripts/common.sh scripts/build.sh
- [ ] rg -n "Phase 7|schedule|cron|clock|missed|resync|curl" README.md scripts/common.sh scripts/validate.sh
- [ ] Human checkpoint approved with Phase 7 reset-then-degrade, schedule-management, manual-versus-scheduled, and runtime scenarios recorded
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Authenticated schedule lifecycle management works end to end without exposing raw internal action IDs in the normal operator flow
- The dedicated scheduler surface shows truthful live scheduler state, clock trust, next run, last result, and recent problems together
- Repo docs and validation scripts clearly separate the automated build gate from the blocking live-device scheduler approval flow

</success_criteria>

<output>
After completion, create `.planning/phases/07-scheduling/07-03-SUMMARY.md`
</output>
