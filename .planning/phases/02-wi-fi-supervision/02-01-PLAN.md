---
phase: 02-wi-fi-supervision
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/CMakeLists.txt
  - app/src/app/bootstrap.c
  - app/src/app/bootstrap.h
  - app/src/network/network_state.h
  - app/src/network/network_supervisor.c
  - app/src/network/network_supervisor.h
  - app/src/network/wifi_lifecycle.c
  - app/src/network/wifi_lifecycle.h
autonomous: true
requirements: [NET-01, NET-03]
user_setup: []
must_haves:
  truths:
    - "Wi-Fi lifecycle ownership moves behind a dedicated supervisor boundary instead of remaining a direct bootstrap sequence."
    - "Connectivity state and last network failure are explicit named contracts, not just inferred from raw booleans."
    - "Low-level Wi-Fi callbacks remain lightweight and feed supervisor-owned state transitions rather than embedding retry policy inline."
  artifacts:
    - path: "app/src/network/network_supervisor.h"
      provides: "Dedicated network supervisor public contract"
      min_lines: 20
      contains: "network_supervisor"
    - path: "app/src/network/network_supervisor.c"
      provides: "Supervisor initialization and startup-ownership implementation"
      min_lines: 80
      contains: "network_supervisor_"
    - path: "app/src/network/network_state.h"
      provides: "Named connectivity state and structured failure contract"
      min_lines: 30
      contains: "NETWORK_CONNECTIVITY_"
  key_links:
    - from: "app/src/app/bootstrap.c"
      to: "app/src/network/network_supervisor.h"
      via: "bootstrap-owned handoff into supervisor lifecycle"
      pattern: 'network_supervisor_'
    - from: "app/src/network/network_supervisor.c"
      to: "app/src/network/wifi_lifecycle.h"
      via: "supervisor delegates low-level Wi-Fi lifecycle actions"
      pattern: 'wifi_lifecycle_'
    - from: "app/src/network/network_supervisor.c"
      to: "app/src/network/network_state.h"
      via: "supervisor publishes named connectivity state"
      pattern: 'NETWORK_CONNECTIVITY_|last_failure'
---

<objective>
Create the dedicated network supervisor boundary and make connectivity state a first-class runtime contract.

Purpose: Give Phase 2 a real owner for startup and ongoing network state before reconnect policy and validation scenarios are added.
Output: A new supervisor module, explicit status/failure types, and bootstrap integration that hands lifecycle control to the supervisor.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wi-fi-supervision/02-CONTEXT.md
@.planning/phases/02-wi-fi-supervision/02-RESEARCH.md
@.planning/phases/02-wi-fi-supervision/02-VALIDATION.md
@app/CMakeLists.txt
@app/Kconfig
@app/src/app/bootstrap.c
@app/src/app/bootstrap.h
@app/src/network/network_state.h
@app/src/network/wifi_lifecycle.c
@app/src/network/wifi_lifecycle.h
@app/src/network/reachability.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the supervisor-facing status and failure contract</name>
  <files>app/src/network/network_state.h, app/src/network/network_supervisor.h</files>
  <action>Extend the networking runtime state with a named connectivity-state enum and a structured last-failure payload that captures stage plus reason. Add a narrow public supervisor API for initialization, startup, and status-oriented interaction. Keep the contract operator-meaningful and future-panel friendly instead of leaking low-level implementation details everywhere.</action>
  <verify>
    <automated>test -f app/src/network/network_supervisor.h && rg -n "NETWORK_CONNECTIVITY_|last_failure|failure_stage|network_supervisor" app/src/network/network_state.h app/src/network/network_supervisor.h</automated>
  </verify>
  <done>The runtime state exposes a clear connectivity contract and the repo has a dedicated supervisor API to own Wi-Fi supervision behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce the supervisor implementation and bootstrap handoff</name>
  <files>app/src/network/network_supervisor.c, app/src/app/bootstrap.c, app/src/app/bootstrap.h, app/CMakeLists.txt</files>
  <action>Create the supervisor implementation, register it in the build, and update bootstrap so it initializes and starts the supervisor instead of sequencing Wi-Fi readiness, connect, DHCP, and reachability directly. Bootstrap should become the handoff point into supervision, not the long-term owner of network policy.</action>
  <verify>
    <automated>test -f app/src/network/network_supervisor.c && rg -n "network_supervisor_" app/src/app/bootstrap.c app/src/network/network_supervisor.c && ! rg -n "wifi_lifecycle_wait_for_connection_and_ipv4|reachability_check_host" app/src/app/bootstrap.c</automated>
  </verify>
  <done>Bootstrap hands control to the supervisor and the new networking module is part of the build with an explicit ownership boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Expand low-level lifecycle events without putting policy in callbacks</name>
  <files>app/src/network/wifi_lifecycle.c, app/src/network/wifi_lifecycle.h, app/src/network/network_supervisor.c</files>
  <action>Keep the Wi-Fi lifecycle module focused on low-level events and connect primitives, but add any missing event coverage and helper hooks the supervisor needs for precise state transitions. Specifically cover DHCP-loss edges and keep callback behavior lightweight so retry and reachability policy live in the supervisor instead of NET_MGMT-thread callbacks.</action>
  <verify>
    <automated>rg -n "NET_EVENT_IPV4_DHCP_STOP|NET_EVENT_IPV4_ADDR_DEL|NET_EVENT_WIFI_DISCONNECT_RESULT" app/src/network/wifi_lifecycle.c && rg -n "wifi_lifecycle_|network_supervisor_" app/src/network/wifi_lifecycle.h app/src/network/network_supervisor.c</automated>
  </verify>
  <done>The lifecycle layer captures the relevant Wi-Fi and IPv4 edges, while the supervisor remains the only owner of higher-level state transitions and policy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/validate.sh
- [ ] rg -n "NETWORK_CONNECTIVITY_|last_failure|failure_stage" app/src/network/network_state.h
- [ ] rg -n "network_supervisor_" app/src/app/bootstrap.c app/src/network/network_supervisor.c app/src/network/network_supervisor.h
- [ ] ! rg -n "wifi_lifecycle_wait_for_connection_and_ipv4|reachability_check_host" app/src/app/bootstrap.c
- [ ] rg -n "NET_EVENT_IPV4_DHCP_STOP|NET_EVENT_IPV4_ADDR_DEL|NET_EVENT_WIFI_DISCONNECT_RESULT" app/src/network/wifi_lifecycle.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bootstrap delegates network lifecycle ownership to a dedicated supervisor boundary
- The runtime state publishes explicit connectivity and last-failure contracts for downstream consumers
- Low-level callbacks stay lightweight and do not own reconnect or reachability policy
</success_criteria>

<output>
After completion, create `.planning/phases/02-wi-fi-supervision/02-01-SUMMARY.md`
</output>
