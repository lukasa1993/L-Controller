---
phase: 02-wi-fi-supervision
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - README.md
  - scripts/common.sh
  - scripts/validate.sh
autonomous: false
requirements: [NET-01, NET-02, NET-03]
user_setup: []
must_haves:
  truths:
    - "The repo keeps a single automated validation entrypoint for fast build-first feedback while clearly separating device-only network scenario sign-off."
    - "Phase 2 validation explicitly covers healthy, degraded-retrying, upstream-degraded, and recovered states instead of only the happy path."
    - "Final phase approval depends on a human-confirmed device run that records the observed network-state behavior and failure visibility."
  artifacts:
    - path: "README.md"
      provides: "Documented Phase 2 validation flow and expected network-state scenarios"
      contains: "upstream"
    - path: "scripts/validate.sh"
      provides: "Phase-safe automated validation entrypoint"
      min_lines: 20
      contains: "build.sh"
    - path: "scripts/common.sh"
      provides: "Shared validation marker or checklist helpers"
      contains: "marker"
  key_links:
    - from: "scripts/validate.sh"
      to: "scripts/build.sh"
      via: "delegated automated build validation"
      pattern: 'build\.sh'
    - from: "README.md"
      to: "scripts/flash.sh"
      via: "documented device validation sequence"
      pattern: 'flash\.sh|console\.sh'
---

<objective>
Validate the supervisor against common Wi-Fi failure modes and document the sign-off flow.

Purpose: Preserve fast automated feedback while making the required device-level network scenarios explicit, repeatable, and blocking for final phase approval.
Output: Updated validation guidance, a build-first automated path, and a recorded human checkpoint for healthy, degraded, upstream-degraded, and recovery behavior.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
@/Users/l/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wi-fi-supervision/02-CONTEXT.md
@.planning/phases/02-wi-fi-supervision/02-RESEARCH.md
@.planning/phases/02-wi-fi-supervision/02-VALIDATION.md
@README.md
@scripts/build.sh
@scripts/common.sh
@scripts/doctor.sh
@scripts/flash.sh
@scripts/console.sh
@scripts/validate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update validation guidance for supervisor states and failure scenarios</name>
  <files>README.md, scripts/common.sh, scripts/validate.sh</files>
  <action>Update the repo documentation and validation messaging so Phase 2 explicitly distinguishes automated build validation from device-only state-machine verification. Document the expected healthy, degraded-retrying, upstream-degraded, and recovery scenarios, and keep the automated path safe for frequent iteration.</action>
  <verify>
    <automated>bash -n scripts/validate.sh scripts/common.sh && rg -n "healthy|degraded|retry|upstream|validate\.sh|flash\.sh|console\.sh" README.md scripts/common.sh scripts/validate.sh</automated>
  </verify>
  <done>The repo explains how to validate the supervisor states and preserves a single fast automated entrypoint for normal development loops.</done>
</task>

<task type="auto">
  <name>Task 2: Stage the device checklist and preserve the automated build path</name>
  <files>README.md, scripts/validate.sh, scripts/common.sh</files>
  <action>Ensure the documented device checklist covers both degraded and recovery behavior rather than only the healthy path, and keep `./scripts/validate.sh` positioned as the canonical wave-level automated validation command. The phase-level verification block should still run the full automated path before completion.</action>
  <verify>
    <automated>bash -n scripts/validate.sh scripts/common.sh && rg -n "build\.sh|healthy|degraded|recovery|upstream" README.md scripts/validate.sh scripts/common.sh</automated>
  </verify>
  <done>The automated validation path remains intact and the device scenario checklist is ready for the blocking final checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the supervisor behavior on real hardware</name>
  <files>README.md, scripts/flash.sh, scripts/console.sh</files>
  <action>Collect final real-device confirmation that the Wi-Fi supervisor behaves correctly across healthy, degraded, and recovered states before Phase 2 can be considered complete.</action>
  <verify>Human checkpoint approved with the required healthy, degraded, upstream-degraded, and recovery scenarios observed, or the failing scenario is recorded.</verify>
  <done>Phase 2 hardware validation is explicitly approved or blocked with a concrete scenario-level note.</done>
  <what-built>Phase 2 supervisor validation flow and documented device checklist</what-built>
  <how-to-verify>
    Using the built firmware and the repo scripts, confirm the supervisor behaves correctly on real hardware:
    1. Flash the image with `./scripts/flash.sh`
    2. Open the device console with `./scripts/console.sh`
    3. Verify a normal boot reaches the healthy state markers documented in the repo
    4. Verify a disrupted boot or dependency outage reaches an explicit degraded-retrying or upstream-degraded state instead of hanging forever
    5. Restore the failed dependency path and confirm the device returns to healthy without a full reboot
    6. Confirm the latest failure remains visible until replaced by a newer failure
    7. Record which scenarios passed or failed in the Phase 2 summary
  </how-to-verify>
  <resume-signal>Type "approved" once the Phase 2 device scenarios are confirmed, or describe the failing scenario</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/validate.sh
- [ ] bash -n scripts/validate.sh scripts/common.sh
- [ ] rg -n "healthy|degraded|retry|upstream|flash\.sh|console\.sh" README.md scripts/common.sh scripts/validate.sh
- [ ] Human checkpoint approved with healthy, degraded, upstream-degraded, and recovery scenarios recorded
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo clearly separates automated build validation from device-only supervision sign-off
- Phase 2 validation covers healthy, degraded-retrying, upstream-degraded, and recovered behavior explicitly
- Final approval records the real-device result for each required supervision scenario
</success_criteria>

<output>
After completion, create `.planning/phases/02-wi-fi-supervision/02-03-SUMMARY.md`
</output>
