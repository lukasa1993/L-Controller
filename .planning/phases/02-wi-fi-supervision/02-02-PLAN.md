---
phase: 02-wi-fi-supervision
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/Kconfig
  - app/src/app/app_config.h
  - app/src/app/bootstrap.c
  - app/src/network/network_state.h
  - app/src/network/network_supervisor.c
  - app/src/network/network_supervisor.h
  - app/src/network/reachability.c
  - app/src/network/reachability.h
autonomous: true
requirements: [NET-01, NET-02, NET-03]
user_setup: []
must_haves:
  truths:
    - "Boot no longer hangs forever waiting for full health; the supervisor settles startup into healthy or explicit degraded-retrying state within a bounded window."
    - "Transient Wi-Fi and DHCP loss trigger steady self-healing behavior without a full-device reboot."
    - "LAN-up upstream failure is exposed distinctly from both healthy and disconnected states, and the most recent failure remains published across recovery."
  artifacts:
    - path: "app/src/network/network_supervisor.c"
      provides: "Supervisor retry, startup-window, and reachability-state policy"
      min_lines: 120
      contains: "k_work_delayable"
    - path: "app/src/network/network_state.h"
      provides: "Runtime state with named degraded and failure-reporting fields"
      min_lines: 40
      contains: "LAN_UP_UPSTREAM_DEGRADED"
    - path: "app/Kconfig"
      provides: "Explicit reconnect cadence configuration"
      contains: "APP_WIFI_RETRY_INTERVAL_MS"
  key_links:
    - from: "app/src/network/network_supervisor.c"
      to: "app/src/network/reachability.h"
      via: "supervisor-owned health probe after Wi-Fi and DHCP recovery"
      pattern: 'reachability_check_host'
    - from: "app/src/network/network_supervisor.c"
      to: "app/src/network/network_supervisor.h"
      via: "bounded startup and retry orchestration contract"
      pattern: 'startup|retry|supervisor'
    - from: "app/src/app/bootstrap.c"
      to: "app/src/network/network_supervisor.h"
      via: "startup outcome wait instead of direct connect-and-wait sequencing"
      pattern: 'network_supervisor_'
---

<objective>
Implement the actual supervision policy: bounded startup, steady reconnect behavior, and reachability-aware state reporting.

Purpose: Satisfy the core Phase 2 behavior requirements after the ownership boundary from Plan 1 exists.
Output: A background-capable supervisor that keeps recovering on its own, differentiates degraded states precisely, and preserves the last network failure for downstream consumers.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wi-fi-supervision/02-CONTEXT.md
@.planning/phases/02-wi-fi-supervision/02-RESEARCH.md
@.planning/phases/02-wi-fi-supervision/02-VALIDATION.md
@app/Kconfig
@app/src/app/app_config.h
@app/src/app/bootstrap.c
@app/src/network/network_state.h
@app/src/network/network_supervisor.c
@app/src/network/network_supervisor.h
@app/src/network/wifi_lifecycle.c
@app/src/network/reachability.c
@app/src/network/reachability.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded startup and steady retry scheduling</name>
  <files>app/Kconfig, app/src/app/app_config.h, app/src/app/bootstrap.c, app/src/network/network_supervisor.c, app/src/network/network_supervisor.h</files>
  <action>Reuse the existing Wi-Fi timeout as the startup deadline, add a single explicit build-time reconnect interval, and implement supervisor-owned scheduled retry behavior using Zephyr work primitives instead of blocking loops. Bootstrap should wait only for the bounded startup outcome, while the supervisor keeps running in the background after degraded startup.</action>
  <verify>
    <automated>rg -n "APP_WIFI_RETRY_INTERVAL_MS" app/Kconfig && rg -n "startup|retry|k_work_delayable" app/src/app/app_config.h app/src/network/network_supervisor.c app/src/network/network_supervisor.h</automated>
  </verify>
  <done>The supervisor owns startup-window and retry cadence behavior, and the firmware no longer treats transient startup failure as a reason to stop supervising.</done>
</task>

<task type="auto">
  <name>Task 2: Publish precise degraded states and structured last-failure reporting</name>
  <files>app/src/network/network_state.h, app/src/network/network_supervisor.c, app/src/network/reachability.c, app/src/network/reachability.h</files>
  <action>Drive connectivity state transitions from the supervisor so full health, LAN-up upstream-degraded, and degraded-retrying are unambiguous. Run reachability checks outside NET_MGMT callbacks, preserve the most recent failure stage plus reason until replaced, and clear degraded state immediately when the full healthy path returns.</action>
  <verify>
    <automated>rg -n "LAN_UP_UPSTREAM_DEGRADED|last_failure" app/src/network/network_state.h && rg -n "reachability|healthy|degraded|failure" app/src/network/network_supervisor.c app/src/network/reachability.c</automated>
  </verify>
  <done>The runtime status model distinguishes local link health from upstream dependency failure and retains structured failure context across recovery.</done>
</task>

<task type="auto">
  <name>Task 3: Keep recovery self-healing and stop short of watchdog escalation</name>
  <files>app/src/app/bootstrap.c, app/src/network/network_supervisor.c, app/src/network/network_state.h</files>
  <action>Make disconnect, DHCP-loss, and reachability-failure paths all converge on the same steady recovery behavior without introducing reboot logic, permanent-stuck classification, or watchdog ownership. Keep Phase 2 scoped to supervision and status publication so the later Recovery & Watchdog phase still has a clear job.</action>
  <verify>
    <automated>! rg -n "sys_reboot|watchdog|wdt" app/src/app/bootstrap.c app/src/network/network_supervisor.c && rg -n "retry|degraded|healthy|failure" app/src/network/network_supervisor.c</automated>
  </verify>
  <done>Phase 2 self-heals through supervision alone and does not overreach into reboot escalation or watchdog policy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/validate.sh
- [ ] rg -n "APP_WIFI_RETRY_INTERVAL_MS|k_work_delayable|startup" app/Kconfig app/src/app/app_config.h app/src/network/network_supervisor.c
- [ ] rg -n "LAN_UP_UPSTREAM_DEGRADED|last_failure|healthy|degraded" app/src/network/network_supervisor.c app/src/network/network_state.h
- [ ] rg -n "reachability_check_host" app/src/network/network_supervisor.c app/src/network/reachability.c
- [ ] ! rg -n "sys_reboot|watchdog|wdt" app/src/app/bootstrap.c app/src/network/network_supervisor.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The supervisor settles startup into healthy or degraded-retrying state within a bounded window
- Runtime disconnect and DHCP-loss paths recover through steady retry instead of rebooting the device
- The firmware exposes a distinct LAN-up upstream-degraded state and retains the latest failure context until a newer failure replaces it
</success_criteria>

<output>
After completion, create `.planning/phases/02-wi-fi-supervision/02-02-SUMMARY.md`
</output>
