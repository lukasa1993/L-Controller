---
phase: 06-action-engine-relay-control
plan: "03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - README.md
  - scripts/common.sh
  - scripts/validate.sh
  - app/src/panel/panel_http.c
  - app/src/panel/panel_status.h
  - app/src/panel/panel_status.c
  - app/src/panel/assets/index.html
  - app/src/panel/assets/main.js
autonomous: false
requirements: [RELAY-01, RELAY-02, ACT-02, RELAY-03]
user_setup: []
must_haves:
  truths:
    - "The panel exposes a single authenticated relay toggle and relay-centric command route, but command execution still flows through the action dispatcher."
    - "Status and UI show actual state, remembered desired state, source, and safety or recovery note together, with inline pending or blocked feedback and no routine confirmation dialog."
    - "Validation and docs clearly separate automated build checks from blocking browser, curl, and device sign-off for relay control and safety behavior."
  artifacts:
    - path: "app/src/panel/panel_http.c"
      provides: "Thin authenticated relay command route plus status-route integration that delegates into the action dispatcher"
      min_lines: 260
      contains: "/api/relay"
    - path: "app/src/panel/panel_status.c"
      provides: "Live relay payload with actual state, desired state, source, and safety note"
      min_lines: 170
      contains: "actual"
    - path: "app/src/panel/assets/main.js"
      provides: "Single-toggle relay UI with inline pending and failure feedback tied to live status refresh"
      min_lines: 260
      contains: "relay"
    - path: "scripts/common.sh"
      provides: "Phase 6 curl commands, scenario labels, and blocking device checklist helpers"
      contains: "Phase 6"
  key_links:
    - from: "app/src/panel/panel_http.c"
      to: "app/src/actions/actions.h"
      via: "authenticated relay route maps desired state into internal action IDs and delegates to the dispatcher"
      pattern: 'action_dispatcher_'
    - from: "app/src/panel/panel_status.c"
      to: "app/src/relay/relay.h"
      via: "aggregate status renders live relay runtime state instead of placeholder persistence-only copy"
      pattern: 'relay_service_'
    - from: "app/src/panel/assets/main.js"
      to: "app/src/panel/panel_http.c"
      via: "frontend submits one relay-centric command and then refreshes from aggregate status"
      pattern: '/api/relay|/api/status'
    - from: "scripts/validate.sh"
      to: "scripts/common.sh"
      via: "automated output and blocking manual checklist stay aligned for Phase 6 approval"
      pattern: 'phase6|Phase 6'
---

<objective>
Wire panel and API relay commands through the action engine and validate Phase 6 end to end.

Purpose: Turn the Phase 5 placeholder relay card into the first real local control surface, keep the external UX relay-centric, and finish the repo-level docs and validation flow needed to prove both live control and safe-state behavior on real hardware.
Output: A thin authenticated relay route, a single-toggle relay UI, live relay status in the aggregate payload, and a documented blocking browser/curl/device sign-off flow for Phase 6.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-action-engine-relay-control/06-CONTEXT.md
@.planning/phases/06-action-engine-relay-control/06-RESEARCH.md
@.planning/phases/06-action-engine-relay-control/06-VALIDATION.md
@app/src/panel/panel_http.c
@app/src/panel/panel_status.h
@app/src/panel/panel_status.c
@app/src/panel/assets/index.html
@app/src/panel/assets/main.js
@app/src/actions/actions.h
@app/src/relay/relay.h
@README.md
@scripts/common.sh
@scripts/validate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand live relay status and add the thin authenticated relay command route</name>
  <files>app/src/panel/panel_http.c, app/src/panel/panel_status.h, app/src/panel/panel_status.c</files>
  <action>Grow the relay portion of `GET /api/status` so it reports implemented or available, actual state, desired state, source, safety note, and pending or blocked reason from the live relay runtime, and increase response-buffer sizing if needed so the new payload cannot truncate silently. Add one exact authenticated relay command route, such as `POST /api/relay/desired-state`, that accepts the desired relay boolean, preserves the existing cookie-session trust model, maps the request to internal action IDs, and delegates to `action_dispatcher`. Keep request buffering small and route-local, and keep scheduling, update, and config mutation routes out of scope.</action>
  <verify>
    <automated>rg -n "/api/relay|desired-state|action_dispatcher_|actual|desired|source|safety|blocked|pending" app/src/panel/panel_http.c app/src/panel/panel_status.c app/src/panel/panel_status.h &amp;&amp; ! rg -n "/api/(schedule|update|config)" app/src/panel/panel_http.c</automated>
  </verify>
  <done>The authenticated HTTP surface can drive relay state through the action engine and report truthful live relay status back to the panel.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder relay card with a single-toggle control and inline feedback</name>
  <files>app/src/panel/assets/index.html, app/src/panel/assets/main.js</files>
  <action>Replace the Phase 5 placeholder relay card with a dedicated single-toggle surface that shows actual state, remembered desired state, source badge, and safety note together. Lock the toggle while commands are in flight or blocked, surface inline pending or failure copy, refresh from `/api/status` after commands, and keep the operator experience relay-centric with no routine confirmation dialog or action-engine terminology.</action>
  <verify>
    <automated>rg -n "relay|toggle|desired|actual|source|safety|pending|blocked" app/src/panel/assets/index.html app/src/panel/assets/main.js &amp;&amp; ! rg -n "confirm\(" app/src/panel/assets/main.js</automated>
  </verify>
  <done>The panel feels like direct relay control while still treating aggregate status as the source of truth after each command.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs and validation flow for Phase 6 relay and safety sign-off</name>
  <files>README.md, scripts/common.sh, scripts/validate.sh</files>
  <action>Document the Phase 6 build-first automated path plus the blocking relay browser, curl, and device checkpoint. Add Phase 6 relay curl commands, scenario labels, ready-state markers, and device checklist helpers to `scripts/common.sh`, then update `scripts/validate.sh` and `README.md` so the repo explains how to validate live relay control and safe-state behavior without inventing a new automation framework.</action>
  <verify>
    <automated>bash -n scripts/validate.sh scripts/common.sh scripts/build.sh &amp;&amp; rg -n "Phase 6|relay|curl|desired|actual|recovery|safe|toggle" README.md scripts/common.sh scripts/validate.sh</automated>
  </verify>
  <done>The repo clearly explains how to build, validate, and approve Phase 6 relay control and safety behavior on a real device.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm live relay control and safety behavior on a real device</name>
  <files>README.md, scripts/flash.sh, scripts/console.sh, scripts/validate.sh</files>
  <action>Collect final browser, curl, and device confirmation that relay control and safety semantics are correct on the local LAN before Phase 6 is considered complete.</action>
  <verify>Human checkpoint approved with relay on, relay off, curl parity, LAN-up upstream-degraded local control, normal reboot policy, recovery-forced relay off, actual-versus-desired mismatch visibility, and pending or blocked UI feedback recorded.</verify>
  <done>Phase 6 control and safety validation is explicitly approved or blocked with scenario-level notes.</done>
  <what-built>Phase 6 live relay control surface, thin relay API, and documented safety-validation flow</what-built>
  <how-to-verify>
    Using the built firmware, the repo scripts, and one or more browsers on the same LAN, confirm Phase 6 relay control behaves correctly:
    1. Run `./scripts/validate.sh` and keep the automated build path green.
    2. Flash the image with `./scripts/flash.sh`.
    3. Open the console with `./scripts/console.sh`, determine the device IP from the boot log, and confirm the normal ready markers plus the relay startup-state log appear.
    4. Log in with the existing cookie-based auth flow and confirm protected access still works before testing control.
    5. Use the Phase 6 relay curl route to drive the first relay on and confirm the physical relay, `GET /api/status`, and the live panel state all agree on actual on plus desired on.
    6. Use the same route to drive the relay off and confirm the physical relay, `GET /api/status`, and the panel all agree on actual off plus desired off.
    7. Open the panel in a browser, log in, toggle the relay on and off, and confirm the control is one tap, uses no routine confirmation dialog, and shows inline pending or failure feedback while locked.
    8. Exercise the local-LAN degraded path, confirm `LAN_UP_UPSTREAM_DEGRADED`, and verify authenticated relay control still works while the device remains locally reachable.
    9. Reboot the device under the normal path and confirm the relay and status payload follow the configured normal-boot policy.
    10. Trigger the recovery-reset or equivalent confirmed-fault path and confirm the next boot forces the relay off until a fresh command is issued.
    11. Confirm the panel makes actual versus remembered desired state visible when safety policy keeps the applied relay off and shows the relevant safety note.
    12. Record which relay on, relay off, curl parity, degraded-local control, normal reboot policy, recovery-forced off, mismatch visibility, and pending or blocked feedback scenarios passed or failed in the Phase 6 summary.
  </how-to-verify>
  <resume-signal>Type "approved" once the Phase 6 relay and safety scenarios are confirmed, or describe the failing scenario</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/validate.sh
- [ ] bash -n scripts/validate.sh scripts/common.sh scripts/build.sh
- [ ] rg -n "/api/relay|desired-state|action_dispatcher_|actual|desired|source|safety|blocked|pending" app/src/panel/panel_http.c app/src/panel/panel_status.c app/src/panel/panel_status.h
- [ ] ! rg -n "/api/(schedule|update|config)" app/src/panel/panel_http.c
- [ ] rg -n "relay|toggle|desired|actual|source|safety|pending|blocked" app/src/panel/assets/index.html app/src/panel/assets/main.js
- [ ] ! rg -n "confirm\(" app/src/panel/assets/main.js
- [ ] rg -n "Phase 6|relay|curl|desired|actual|recovery|safe|toggle" README.md scripts/common.sh scripts/validate.sh
- [ ] Human checkpoint approved with relay-control and safety scenarios recorded
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Authenticated relay control flows through one thin relay-centric HTTP route backed by the action dispatcher
- The panel shows actual state, remembered desired state, source, and safety note together and keeps the toggle honest while commands are pending or blocked
- Repo docs and validation scripts clearly separate the automated build gate from the blocking real-device control and safety sign-off

</success_criteria>

<output>
After completion, create `.planning/phases/06-action-engine-relay-control/06-03-SUMMARY.md`
</output>
