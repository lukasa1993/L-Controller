---
phase: 06-action-engine-relay-control
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/CMakeLists.txt
  - app/prj.conf
  - app/boards/nrf7002dk_nrf5340_cpuapp.overlay
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/relay/relay.h
  - app/src/relay/relay.c
autonomous: true
requirements: [RELAY-03]
user_setup: []
must_haves:
  truths:
    - "Relay hardware control is isolated behind a relay service and driver boundary rather than exposed directly to HTTP handlers or future callers."
    - "Ordinary reboot behavior honors the configured relay reboot policy, while recovery-triggered restart always forces the relay off until a fresh command is issued."
    - "When safety policy keeps the applied relay state different from the remembered desired state, runtime status preserves both values so later panel work can surface the mismatch explicitly."
  artifacts:
    - path: "app/src/relay/relay.c"
      provides: "GPIO-backed relay runtime with startup policy evaluation and live status access"
      min_lines: 180
      contains: "relay_service_"
    - path: "app/boards/nrf7002dk_nrf5340_cpuapp.overlay"
      provides: "Board-owned binding for the first relay output and polarity"
      contains: "relay0"
    - path: "app/src/app/bootstrap.c"
      provides: "Boot ordering that composes relay runtime before the panel relies on live state"
      contains: "panel_http_server_init"
  key_links:
    - from: "app/src/app/bootstrap.c"
      to: "app/src/relay/relay.h"
      via: "boot initializes relay runtime and applies startup policy before HTTP control and status surfaces use it"
      pattern: 'relay_service_'
    - from: "app/src/relay/relay.c"
      to: "app/src/persistence/persistence_types.h"
      via: "startup policy consumes remembered desired state plus the persisted reboot policy"
      pattern: 'PERSISTED_RELAY_REBOOT_POLICY_'
    - from: "app/src/relay/relay.c"
      to: "app/src/recovery/recovery.h"
      via: "recovery reset breadcrumbs influence force-off behavior on boot"
      pattern: 'recovery_manager_last_reset_cause|RECOVERY'
---

<objective>
Build the first real relay runtime, board binding, and safe startup policy for Phase 6.

Purpose: Turn the header-only relay boundary into a real runtime service that owns hardware state, applies the configured boot policy safely, and establishes a live status contract before the panel or future action engine depend on it.
Output: A built relay service with board-owned GPIO binding, explicit ordinary-boot versus recovery-reset behavior, and boot-owned initialization ready for the action pipeline.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-action-engine-relay-control/06-CONTEXT.md
@.planning/phases/06-action-engine-relay-control/06-RESEARCH.md
@.planning/phases/06-action-engine-relay-control/06-VALIDATION.md
@app/CMakeLists.txt
@app/prj.conf
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/persistence/persistence.h
@app/src/persistence/persistence_types.h
@app/src/recovery/recovery.h
@app/src/panel/panel_status.h
@app/src/panel/panel_status.c
@app/src/relay/relay.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add relay build wiring and board-owned first-relay binding</name>
  <files>app/CMakeLists.txt, app/prj.conf, app/boards/nrf7002dk_nrf5340_cpuapp.overlay, app/src/relay/relay.h</files>
  <action>Add the first real relay build surface: register `app/src/relay/relay.c`, enable only the minimal Zephyr GPIO support required, and define the current board's first relay binding through an overlay alias such as `relay0` with polarity owned by board configuration rather than by HTTP or action code. Keep the hardware mapping isolated to the board layer and make missing or invalid binding fail clearly.</action>
  <verify>
    <automated>rg -n "relay\.c|relay_service" app/CMakeLists.txt &amp;&amp; rg -n "CONFIG_GPIO=y" app/prj.conf &amp;&amp; test -f app/boards/nrf7002dk_nrf5340_cpuapp.overlay &amp;&amp; rg -n "relay0|gpios" app/boards/nrf7002dk_nrf5340_cpuapp.overlay</automated>
  </verify>
  <done>The repo builds with a real relay module and a board-owned first-relay binding instead of a placeholder subsystem boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement relay runtime state and explicit startup policy</name>
  <files>app/src/relay/relay.h, app/src/relay/relay.c</files>
  <action>Implement `relay_service` as the sole owner of applied hardware state. Add live runtime status fields for implemented or available, actual applied state, remembered desired state, source, and safety note. Apply startup policy from `persisted_config.relay`: ordinary reboot obeys the configured reboot policy, while recovery-reset boot forces the relay off until a fresh command arrives. Preserve remembered desired state even when safety policy forces the applied state off so later panel work can show both values.</action>
  <verify>
    <automated>test -f app/src/relay/relay.c &amp;&amp; rg -n "reboot_policy|recovery|desired|actual|source|safe|gpio" app/src/relay/relay.c app/src/relay/relay.h</automated>
  </verify>
  <done>The firmware owns a real relay runtime with explicit policy-driven startup behavior instead of inferred state from persistence alone.</done>
</task>

<task type="auto">
  <name>Task 3: Compose relay ownership into boot before panel startup</name>
  <files>app/src/app/app_context.h, app/src/app/bootstrap.c, app/src/relay/relay.h, app/src/relay/relay.c</files>
  <action>Add `relay_service` to `app_context`, initialize it after persistence is loaded and the recovery breadcrumb is available, apply startup policy before `panel_http_server_init()`, and expose a read-only relay status getter that later panel status work can consume. If prior recovery-reset context is needed earlier than the current init order allows, prefer a narrow recovery-breadcrumb helper over moving the full long-lived watchdog runtime earlier. Keep the panel boot contract unchanged for local-LAN availability and log the resolved relay startup state clearly for later validation.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "relay_service_|persisted_config\.relay|recovery_manager_last_reset_cause|panel_http_server_init" app/src/app/bootstrap.c app/src/app/app_context.h app/src/relay/relay.c app/src/relay/relay.h</automated>
  </verify>
  <done>The app boot path owns a real relay runtime and safe startup outcome before any HTTP surface depends on live relay state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "relay\.c|relay_service" app/CMakeLists.txt
- [ ] rg -n "CONFIG_GPIO=y" app/prj.conf
- [ ] test -f app/boards/nrf7002dk_nrf5340_cpuapp.overlay &amp;&amp; rg -n "relay0|gpios" app/boards/nrf7002dk_nrf5340_cpuapp.overlay
- [ ] test -f app/src/relay/relay.c &amp;&amp; rg -n "reboot_policy|recovery|desired|actual|source|safe|gpio" app/src/relay/relay.c app/src/relay/relay.h
- [ ] rg -n "relay_service_|persisted_config\.relay|recovery_manager_last_reset_cause|panel_http_server_init" app/src/app/bootstrap.c app/src/app/app_context.h app/src/relay/relay.c app/src/relay/relay.h
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Relay hardware state is owned by a real relay runtime rather than inferred from persistence or manipulated from HTTP code
- Ordinary reboot policy remains configurable while recovery-reset behavior is stricter and forces relay off until a fresh command
- Runtime state preserves actual versus remembered desired relay state so later panel work can render safety overrides truthfully

</success_criteria>

<output>
After completion, create `.planning/phases/06-action-engine-relay-control/06-01-SUMMARY.md`
</output>
