---
phase: 06-action-engine-relay-control
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/CMakeLists.txt
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/actions/actions.h
  - app/src/actions/actions.c
  - app/src/relay/relay.h
  - app/src/relay/relay.c
autonomous: true
requirements: [ACT-01, ACT-02]
user_setup: []
must_haves:
  truths:
    - "Manual relay commands and future scheduler callers share one action-dispatch execution path instead of touching relay state directly."
    - "The action module exposes a generic execution contract and keeps relay-specific catalog and persistence details behind that boundary."
    - "A clean device has stable built-in relay on and off actions available without a separate setup step, and accepted commands update remembered desired state through persistence."
  artifacts:
    - path: "app/src/actions/actions.c"
      provides: "Dispatcher that resolves action IDs, seeds built-in relay actions, persists desired state, and delegates actuation work"
      min_lines: 180
      contains: "action_dispatcher"
    - path: "app/src/actions/actions.h"
      provides: "Generic action-execution contract with source and result types usable by the panel now and scheduler later"
      contains: "struct action_dispatcher"
    - path: "app/src/app/app_context.h"
      provides: "App-owned dispatcher state composed alongside relay runtime"
      contains: "action_dispatcher"
  key_links:
    - from: "app/src/actions/actions.c"
      to: "app/src/persistence/persistence.h"
      via: "dispatcher seeds and updates the durable action catalog and remembered relay intent through persistence APIs"
      pattern: 'persistence_store_save_(actions|relay|config)'
    - from: "app/src/actions/actions.c"
      to: "app/src/relay/relay.h"
      via: "generic action resolution delegates actual actuation into relay runtime instead of HTTP or persistence code"
      pattern: 'relay_service_'
    - from: "app/src/app/bootstrap.c"
      to: "app/src/actions/actions.h"
      via: "boot composes the dispatcher after relay runtime is available and before HTTP handlers use it"
      pattern: 'action_dispatcher_'
---

<objective>
Implement the generic action model and shared execution pipeline for Phase 6.

Purpose: Insert a real action boundary between callers and relay hardware so manual panel commands and future schedules use the same execution path, built-in relay actions exist on a clean device, and the panel never becomes the owner of business logic or durability updates.
Output: A working `action_dispatcher` with a generic execution contract, reserved built-in relay actions, and one shared path from `action_id` to relay actuation plus desired-state persistence.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-action-engine-relay-control/06-CONTEXT.md
@.planning/phases/06-action-engine-relay-control/06-RESEARCH.md
@.planning/phases/06-action-engine-relay-control/06-VALIDATION.md
@app/CMakeLists.txt
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/actions/actions.h
@app/src/relay/relay.h
@app/src/persistence/persistence.h
@app/src/persistence/persistence_types.h
@app/src/scheduler/scheduler.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the dispatcher contract and compose it into app ownership</name>
  <files>app/CMakeLists.txt, app/src/app/app_context.h, app/src/app/bootstrap.c, app/src/actions/actions.h, app/src/actions/actions.c</files>
  <action>Add `app/src/actions/actions.c` to the build, define a generic dispatcher API with execution source and result types, compose `action_dispatcher` into `app_context`, and initialize it after relay runtime is available. The public contract should stay generic and action-ID-based so future scheduler work can call the same path without learning relay details.</action>
  <verify>
    <automated>test -f app/src/actions/actions.c &amp;&amp; rg -n "action_dispatcher_|action_id|source|execute|result" app/CMakeLists.txt app/src/actions/actions.c app/src/actions/actions.h app/src/app/app_context.h app/src/app/bootstrap.c</automated>
  </verify>
  <done>The app owns a real action-dispatch boundary instead of leaving relay callers to invent their own execution paths.</done>
</task>

<task type="auto">
  <name>Task 2: Seed reserved built-in relay actions for clean-device control</name>
  <files>app/src/actions/actions.h, app/src/actions/actions.c</files>
  <action>During dispatcher startup, ensure two stable built-in relay actions exist for relay off and relay on. Preserve existing user-owned catalog entries, repair missing built-ins when the catalog can safely accommodate them, and fail closed rather than silently changing arbitrary action IDs. Keep the built-ins internal implementation details so the operator still sees a relay-centric control surface instead of action administration.</action>
  <verify>
    <automated>rg -n "relay.*on|relay.*off|built-?in|action_id|persisted_config\.actions|persistence_store_save_actions" app/src/actions/actions.c app/src/actions/actions.h app/src/persistence/persistence.h</automated>
  </verify>
  <done>A clean device has stable on and off actions available for Phase 6 without a separate action-setup workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the shared execution path and desired-state persistence</name>
  <files>app/src/actions/actions.h, app/src/actions/actions.c, app/src/relay/relay.h, app/src/relay/relay.c</files>
  <action>Implement the shared execution path that validates the resolved action, rejects missing or disabled actions, delegates actual actuation into `relay_service`, persists updated remembered desired state only when a command is accepted, and records source metadata such as manual panel, safety policy, or future scheduler. Do not let HTTP or future scheduler work call GPIO or persistence directly.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "action_dispatcher_execute|enabled|last_desired_state|source|relay_service_" app/src/actions/actions.c app/src/actions/actions.h app/src/relay/relay.c app/src/relay/relay.h &amp;&amp; ! rg -n "gpio_pin_(configure|set)" app/src/panel/panel_http.c</automated>
  </verify>
  <done>The firmware now has one shared action-ID execution path that future schedules can reuse instead of duplicating relay logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] test -f app/src/actions/actions.c
- [ ] rg -n "action_dispatcher_|action_id|source|execute|result" app/CMakeLists.txt app/src/actions/actions.c app/src/actions/actions.h app/src/app/app_context.h app/src/app/bootstrap.c
- [ ] rg -n "relay.*on|relay.*off|built-?in|action_id|persisted_config\.actions|persistence_store_save_actions" app/src/actions/actions.c app/src/actions/actions.h app/src/persistence/persistence.h
- [ ] rg -n "action_dispatcher_execute|enabled|last_desired_state|source|relay_service_" app/src/actions/actions.c app/src/actions/actions.h app/src/relay/relay.c app/src/relay/relay.h
- [ ] ! rg -n "gpio_pin_(configure|set)" app/src/panel/panel_http.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Manual relay control and future scheduler callers share one dispatcher-owned execution path
- A clean device gets stable built-in relay actions without a separate setup step
- Desired relay state persists through the action boundary instead of being written directly from the panel or hardware layer

</success_criteria>

<output>
After completion, create `.planning/phases/06-action-engine-relay-control/06-02-SUMMARY.md`
</output>
