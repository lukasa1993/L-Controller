---
phase: 09-configured-action-model-and-panel-management
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/src/actions/actions.h
  - app/src/actions/actions.c
  - app/src/panel/panel_http.c
  - app/src/panel/panel_status.h
  - app/src/panel/panel_status.c
  - app/src/persistence/persistence.h
  - app/src/persistence/persistence.c
autonomous: true
requirements: [ACT-03, ACT-04, ACT-05, BIND-01, BIND-02]
user_setup: []
must_haves:
  truths:
    - "Authenticated operators can create, view, and edit configured relay actions through exact-route JSON APIs instead of relying on fixed relay booleans or hard-coded public keys."
    - "The API returns truthful catalog state including display name, enabled state, approved-output summary, and usability badges such as `Ready`, `Disabled`, and `Needs attention`."
    - "Mutation flows stay inside the existing atomic persistence-save contract and do not accidentally reintroduce manual execution or schedule-catalog coupling before Phase 10."
  artifacts:
    - path: "app/src/panel/panel_http.c"
      provides: "Authenticated configured-action snapshot, create, and update route handlers with field-level validation"
      min_lines: 2600
      contains: "/api/actions"
    - path: "app/src/panel/panel_status.c"
      provides: "Configured-action serialization helpers and operator-facing usability text"
      min_lines: 650
      contains: "Needs attention"
    - path: "app/src/actions/actions.c"
      provides: "Server-side ID generation, uniqueness enforcement, approved-output validation, and usability evaluation"
      min_lines: 320
      contains: "normalize"
  key_links:
    - from: "app/src/panel/panel_http.c"
      to: "app/src/actions/actions.c"
      via: "HTTP handlers delegate ID generation, binding validation, and usability evaluation into the action catalog boundary"
      pattern: 'normalize|binding|action'
    - from: "app/src/panel/panel_http.c"
      to: "app/src/persistence/persistence.c"
      via: "catalog mutations save through the same atomic persistence path used by other operator-managed sections"
      pattern: 'persistence_store_save_actions'
    - from: "app/src/panel/panel_status.c"
      to: "app/src/actions/actions.c"
      via: "operator-facing snapshot data comes from configured-action truth instead of relay-only placeholders"
      pattern: 'Ready|Disabled|Needs attention|action'
---

<objective>
Deliver the authenticated configured-action API and snapshot truth for Phase 9.

Purpose: Give the panel a real configured-action contract it can render and mutate, using the same authenticated exact-route patterns and atomic persistence guarantees the repo already trusts.
Output: `/api/actions` snapshot and mutation routes, server-generated operator-safe IDs, approved-output validation, and truthful usability-state serialization.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-configured-action-model-and-panel-management/09-CONTEXT.md
@.planning/phases/09-configured-action-model-and-panel-management/09-RESEARCH.md
@.planning/phases/09-configured-action-model-and-panel-management/09-VALIDATION.md
@.planning/phases/09-configured-action-model-and-panel-management/09-01-PLAN.md
@app/src/actions/actions.h
@app/src/actions/actions.c
@app/src/panel/panel_http.c
@app/src/panel/panel_status.h
@app/src/panel/panel_status.c
@app/src/persistence/persistence.h
@app/src/persistence/persistence.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a configured-action snapshot endpoint with truthful operator-facing status</name>
  <files>app/src/panel/panel_http.c, app/src/panel/panel_status.h, app/src/panel/panel_status.c, app/src/actions/actions.c</files>
  <action>Add an authenticated snapshot route for configured actions that returns the catalog plus empty-state metadata and usability truth the Actions page needs. The snapshot should include display name, generated action ID, enabled state, approved-output summary, and short operator-facing usability language: `Ready`, `Disabled`, and `Needs attention`.</action>
  <verify>
    <automated>rg -n "/api/actions|configured action|Ready|Disabled|Needs attention|output summary" app/src/panel/panel_http.c app/src/panel/panel_status.h app/src/panel/panel_status.c app/src/actions/actions.c</automated>
  </verify>
  <done>The panel can fetch one truthful configured-action snapshot instead of inferring state from relay-only placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Implement authenticated create and edit handlers with server-owned ID generation</name>
  <files>app/src/panel/panel_http.c, app/src/actions/actions.h, app/src/actions/actions.c</files>
  <action>Add exact-route create and update handlers that accept the operator-facing fields from Phase 9, generate stable operator-safe IDs from the display name on the device, enforce uniqueness and approved-output validation, and return field-specific errors when the request is invalid. Keep the create and edit contract command-centric: one record equals one configured relay command.</action>
  <verify>
    <automated>rg -n "operator-safe|normalize|collision|binding|invalid-action|invalid-binding|display name|create|update" app/src/panel/panel_http.c app/src/actions/actions.h app/src/actions/actions.c</automated>
  </verify>
  <done>The configured-action API owns ID generation and input validation instead of trusting browser-authored IDs or raw GPIO data.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve atomic save-and-reload behavior for configured-action mutations</name>
  <files>app/src/panel/panel_http.c, app/src/actions/actions.c, app/src/persistence/persistence.h, app/src/persistence/persistence.c</files>
  <action>Route accepted configured-action mutations through the existing atomic persistence save path, then refresh runtime truth immediately so the UI sees server-owned state on the next fetch. Keep this phase limited to catalog CRUD: do not wire these mutations into manual execution or the schedule catalog yet.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "persistence_store_save_actions|reload|configured action|enabled state|usability" app/src/panel/panel_http.c app/src/actions/actions.c app/src/persistence/persistence.h app/src/persistence/persistence.c</automated>
  </verify>
  <done>Configured-action create and edit flows persist durably and refresh cleanly without spilling into Phase 10 execution work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "/api/actions|configured action|Ready|Disabled|Needs attention|output summary" app/src/panel/panel_http.c app/src/panel/panel_status.h app/src/panel/panel_status.c app/src/actions/actions.c
- [ ] rg -n "operator-safe|normalize|collision|binding|invalid-action|invalid-binding|display name|create|update" app/src/panel/panel_http.c app/src/actions/actions.h app/src/actions/actions.c
- [ ] rg -n "persistence_store_save_actions|reload|configured action|enabled state|usability" app/src/panel/panel_http.c app/src/actions/actions.c app/src/persistence/persistence.h app/src/persistence/persistence.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Authenticated operators can create, view, and edit configured relay actions through exact-route JSON APIs
- The API returns truthful catalog usability and binding summary instead of relay-only placeholders
- Action CRUD stays atomic and does not prematurely wire manual execution or schedule-catalog adoption

</success_criteria>

<output>
After completion, create `.planning/phases/09-configured-action-model-and-panel-management/09-02-SUMMARY.md`
</output>
