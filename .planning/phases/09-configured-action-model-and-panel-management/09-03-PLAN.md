---
phase: 09-configured-action-model-and-panel-management
plan: "03"
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - app/src/panel/assets/index.html
  - app/src/panel/assets/main.js
  - tests/panel-login.spec.js
  - tests/helpers/panel-target.js
  - README.md
autonomous: true
requirements: [ACT-03, ACT-04, ACT-05, MIGR-03]
user_setup: []
must_haves:
  truths:
    - "The Actions page becomes a configured-action management catalog with a create-first empty state instead of fake-ready manual relay buttons."
    - "Operators can create and edit configured actions from the panel and see truthful `Ready`, `Disabled`, or `Needs attention` state from server-owned data."
    - "Phase 9 keeps execution and schedule-catalog adoption explicitly deferred, so the UI does not pretend configured actions are manually runnable or scheduler-selectable yet."
  artifacts:
    - path: "app/src/panel/assets/main.js"
      provides: "Configured-action catalog rendering, create or edit form state, and honest deferral of execution"
      min_lines: 2100
      contains: "No configured actions"
    - path: "app/src/panel/assets/index.html"
      provides: "Actions-page structure for the configured-action catalog and form controls"
      contains: "actions-view"
    - path: "tests/panel-login.spec.js"
      provides: "Browser smoke updated so the authenticated shell still lands correctly after the Actions page becomes a management surface"
      contains: "Actions"
  key_links:
    - from: "app/src/panel/assets/main.js"
      to: "app/src/panel/panel_http.c"
      via: "the Actions surface reads the configured-action snapshot and posts create or update mutations through exact routes"
      pattern: '/api/actions'
    - from: "app/src/panel/assets/main.js"
      to: "app/src/panel/panel_status.c"
      via: "UI badges and summaries come from server-owned usability truth rather than hard-coded relay assumptions"
      pattern: 'Ready|Disabled|Needs attention'
    - from: "tests/panel-login.spec.js"
      to: "tests/helpers/panel-target.js"
      via: "browser validation still discovers and authenticates against the live panel after the Actions surface changes shape"
      pattern: 'resolvePanelBaseUrl|resolvePanelCredentials'
---

<objective>
Replace the placeholder Actions page with the real configured-action management surface for Phase 9.

Purpose: Turn the current honest placeholder into a truthful operator workflow for viewing, creating, and editing configured relay actions while keeping manual execution and schedule sharing explicitly deferred to Phase 10.
Output: A create-first action catalog UI, create or edit form wiring to the new API, and verification assets updated for the management surface.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-configured-action-model-and-panel-management/09-CONTEXT.md
@.planning/phases/09-configured-action-model-and-panel-management/09-RESEARCH.md
@.planning/phases/09-configured-action-model-and-panel-management/09-VALIDATION.md
@.planning/phases/09-configured-action-model-and-panel-management/09-02-PLAN.md
@app/src/panel/assets/index.html
@app/src/panel/assets/main.js
@app/src/panel/panel_http.c
@app/src/panel/panel_status.c
@tests/panel-login.spec.js
@tests/helpers/panel-target.js
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder Actions surface with a create-first configured-action catalog</name>
  <files>app/src/panel/assets/index.html, app/src/panel/assets/main.js</files>
  <action>Remove the fake-ready manual relay presentation from the Actions page and replace it with the configured-action management catalog. The page should show a create-first empty state when no actions exist, then render catalog rows or cards with display name, bound output summary, enabled state, and usability badges once actions are present.</action>
  <verify>
    <automated>rg -n "No configured actions|Create action|Edit action|Needs attention|Ready|Disabled" app/src/panel/assets/index.html app/src/panel/assets/main.js</automated>
  </verify>
  <done>The Actions page is now a management surface instead of a placeholder or direct relay-toggle page.</done>
</task>

<task type="auto">
  <name>Task 2: Wire panel create and edit flows to the configured-action API</name>
  <files>app/src/panel/assets/main.js</files>
  <action>Hook the Actions page form state to the new configured-action API so operators can create and edit actions entirely from the panel. Refresh from server truth after each accepted mutation, keep internal action IDs out of the normal operator flow, and surface only the short usability language and friendly binding summary defined in phase context.</action>
  <verify>
    <automated>rg -n "/api/actions|renderActions|action catalog|output summary|enabled|display name" app/src/panel/assets/main.js</automated>
  </verify>
  <done>The panel supports create and edit workflows for configured actions while staying server-truth-first.</done>
</task>

<task type="auto">
  <name>Task 3: Update verification assets and keep execution and schedules explicitly deferred</name>
  <files>app/src/panel/assets/main.js, tests/panel-login.spec.js, tests/helpers/panel-target.js, README.md</files>
  <action>Refresh the existing browser smoke or related documentation so the authenticated shell remains verified after the Actions page becomes a management surface. Keep the Phase 9 deferral explicit: the UI should not expose direct execute controls or claim configured actions are schedule-selectable until Phase 10 lands the shared execution and schedule-catalog work.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "/api/relay/desired-state|relay-on|relay-off|No configured actions|Actions|configured action" app/src/panel/assets/main.js tests/panel-login.spec.js tests/helpers/panel-target.js README.md</automated>
  </verify>
  <done>The management surface is covered by updated verification assets and remains honest about what is still deferred to Phase 10.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "No configured actions|Create action|Edit action|Needs attention|Ready|Disabled" app/src/panel/assets/index.html app/src/panel/assets/main.js
- [ ] rg -n "/api/actions|renderActions|action catalog|output summary|enabled|display name" app/src/panel/assets/main.js
- [ ] rg -n "/api/relay/desired-state|relay-on|relay-off|No configured actions|Actions|configured action" app/src/panel/assets/main.js tests/panel-login.spec.js tests/helpers/panel-target.js README.md
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Actions page is a truthful configured-action management surface with create and edit flows
- The UI shows operator-facing status and binding summaries from server-owned data
- Manual execution and schedule-catalog adoption remain explicitly deferred until Phase 10 instead of being implied by the UI

</success_criteria>

<output>
After completion, create `.planning/phases/09-configured-action-model-and-panel-management/09-03-SUMMARY.md`
</output>
