---
phase: 09-configured-action-model-and-panel-management
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/actions/actions.h
  - app/src/actions/actions.c
  - app/src/persistence/persistence.h
  - app/src/persistence/persistence_types.h
  - app/src/persistence/persistence.c
  - app/src/relay/relay.h
  - app/src/relay/relay.c
  - app/src/app/bootstrap.c
autonomous: true
requirements: [BIND-01, BIND-02, MIGR-01, MIGR-03]
user_setup: []
must_haves:
  truths:
    - "Configured relay actions become durable records with operator-facing name, stable operator-safe ID, enabled state, command meaning, and approved-output binding instead of hidden built-in relay entries."
    - "Boot no longer seeds or depends on hidden `relay0.on` and `relay0.off` actions, and legacy built-in action records are handled explicitly rather than silently restored."
    - "Approved output validation is firmware-owned and rejects unsafe bindings before they become usable, while legacy schedule references are preserved only as explicit compatibility behavior so Phase 9 does not wipe existing schedules before Phase 10 owns their full migration."
  artifacts:
    - path: "app/src/persistence/persistence_types.h"
      provides: "Configured action record schema with name, binding, command metadata, and future-extensible action fields"
      min_lines: 210
      contains: "output_key"
    - path: "app/src/actions/actions.c"
      provides: "Configured-action catalog helpers for ID normalization, approved-output validation, and legacy built-in normalization"
      min_lines: 280
      contains: "legacy"
    - path: "app/src/relay/relay.c"
      provides: "Firmware-owned approved-output lookup surface rooted in the existing relay definitions"
      contains: "relay0"
  key_links:
    - from: "app/src/persistence/persistence.c"
      to: "app/src/actions/actions.c"
      via: "action load and save logic normalize legacy built-ins while preserving legacy schedule references as compatibility-only behavior instead of resetting unrelated schedule data"
      pattern: 'relay0\.on|relay0\.off|legacy|migration|schedule'
    - from: "app/src/actions/actions.c"
      to: "app/src/relay/relay.h"
      via: "configured-action validation accepts only firmware-approved output bindings rather than raw GPIO input"
      pattern: 'output|binding|relay'
    - from: "app/src/app/bootstrap.c"
      to: "app/src/actions/actions.h"
      via: "boot composes the configured action catalog without unconditional built-in seeding"
      pattern: 'action_dispatcher_'
---

<objective>
Establish the configured action schema, approved-output contract, and explicit migration path for Phase 9.

Purpose: Replace the current built-in relay action assumptions with a durable configured-action catalog that the rest of the panel can trust, while keeping legacy built-ins explicit and preserving old schedule references safely until Phase 10 can migrate them fully.
Output: A richer configured-action persistence contract, firmware-owned approved-output validation, and boot behavior that no longer seeds hidden relay actions while still preserving legacy schedule references as compatibility-only truth.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-configured-action-model-and-panel-management/09-CONTEXT.md
@.planning/phases/09-configured-action-model-and-panel-management/09-RESEARCH.md
@.planning/phases/09-configured-action-model-and-panel-management/09-VALIDATION.md
@app/src/app/bootstrap.c
@app/src/actions/actions.h
@app/src/actions/actions.c
@app/src/persistence/persistence.h
@app/src/persistence/persistence_types.h
@app/src/persistence/persistence.c
@app/src/relay/relay.h
@app/src/relay/relay.c
@app/boards/nrf7002dk_nrf5340_cpuapp.overlay
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redefine the action catalog around configured relay records and approved outputs</name>
  <files>app/src/actions/actions.h, app/src/actions/actions.c, app/src/persistence/persistence_types.h, app/src/relay/relay.h, app/src/relay/relay.c</files>
  <action>Expand the action model from the current built-in relay boolean into a configured record that can carry the operator-facing display name, generated stable action ID, enabled state, command meaning, and approved output binding. Keep one record equal to one executable command, keep the model extensible enough for future action types, and expose only firmware-approved output choices rather than raw GPIO entry.</action>
  <verify>
    <automated>rg -n "display_name|output_key|command|enabled|action_id" app/src/persistence/persistence_types.h app/src/actions/actions.h app/src/actions/actions.c app/src/relay/relay.h app/src/relay/relay.c</automated>
  </verify>
  <done>The repo has a real configured-action record and a firmware-owned approved-output contract instead of hidden built-in relay assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Replace unconditional built-in seeding with explicit legacy normalization</name>
  <files>app/src/actions/actions.c, app/src/persistence/persistence.c, app/src/app/bootstrap.c</files>
  <action>Remove the dispatcher behavior that auto-seeds `relay0.on` and `relay0.off` as the primary action model. Add an explicit legacy-action normalization or migration path that handles existing built-in action records safely, logs what happened, and prevents boot from silently restoring hidden relay execution. Keep legacy built-ins out of the operator-facing catalog while making any retained compatibility behavior explicit in firmware internals.</action>
  <verify>
    <automated>rg -n "relay0\\.on|relay0\\.off|legacy|migration|normalize|built-in" app/src/actions/actions.c app/src/persistence/persistence.c app/src/app/bootstrap.c</automated>
  </verify>
  <done>Legacy built-in action IDs are handled explicitly and boot no longer depends on hidden relay seeding.</done>
</task>

<task type="auto">
  <name>Task 3: Keep configured-action saves atomic and preserve legacy schedule references safely</name>
  <files>app/src/persistence/persistence.h, app/src/persistence/persistence.c, app/src/actions/actions.c, app/src/scheduler/scheduler.c</files>
  <action>Wire the richer action catalog through the existing atomic persistence save path, keep action validation and scheduler reference checks coherent, and avoid any migration shortcut that resets unrelated sections just to make the action schema evolve. Add the minimum compatibility behavior Phase 9 needs so existing schedules that still reference legacy built-in IDs survive boot and save validation without pretending the scheduler already understands the full configured-action model.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "persistence_store_save_actions|persistence_store_load_actions|persisted_action_catalog_valid|action_id|schedule|legacy" app/src/persistence/persistence.c app/src/persistence/persistence.h app/src/actions/actions.c app/src/scheduler/scheduler.c</automated>
  </verify>
  <done>The configured-action model persists atomically through the existing store without forcing a blunt reset of unrelated sections, and existing legacy schedule references survive Phase 9 safely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "display_name|output_key|command|enabled|action_id" app/src/persistence/persistence_types.h app/src/actions/actions.h app/src/actions/actions.c app/src/relay/relay.h app/src/relay/relay.c
- [ ] rg -n "relay0\\.on|relay0\\.off|legacy|migration|normalize|built-in" app/src/actions/actions.c app/src/persistence/persistence.c app/src/app/bootstrap.c
- [ ] rg -n "persistence_store_save_actions|persistence_store_load_actions|persisted_action_catalog_valid|action_id|schedule|legacy" app/src/persistence/persistence.c app/src/persistence/persistence.h app/src/actions/actions.c app/src/scheduler/scheduler.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo persists configured relay actions as operator-managed records rather than boot-seeded built-ins
- Firmware-owned approved-output validation blocks unsafe bindings before they become usable
- Boot handles legacy built-in action data explicitly, never falls back to hidden built-in execution as the primary model, and does not wipe legacy schedules during the Phase 9 cutover

</success_criteria>

<output>
After completion, create `.planning/phases/09-configured-action-model-and-panel-management/09-01-SUMMARY.md`
</output>
