---
phase: 01-foundation-refactor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/CMakeLists.txt
  - app/src/app/app_config.h
  - app/src/app/app_context.h
  - app/src/app/bootstrap.h
  - app/src/network/network_state.h
  - app/src/network/wifi_lifecycle.h
  - app/src/network/reachability.h
  - app/src/recovery/recovery.h
  - app/src/panel/panel_http.h
  - app/src/panel/panel_auth.h
  - app/src/actions/actions.h
  - app/src/scheduler/scheduler.h
  - app/src/persistence/persistence.h
  - app/src/ota/ota.h
  - app/src/relay/relay.h
autonomous: true
requirements: [PLAT-01, PLAT-03]
user_setup: []
must_haves:
  truths:
    - "The firmware source tree has clear module homes for bootstrap, networking, recovery, panel/auth, actions, scheduler, persistence, OTA, and relay work."
    - "Shared application and networking contracts are explicit typed interfaces rather than hidden cross-file globals."
  artifacts:
    - path: "app/src/app/app_context.h"
      provides: "Top-level typed application context contract"
      min_lines: 20
    - path: "app/src/network/network_state.h"
      provides: "Typed networking runtime state contract"
      min_lines: 20
    - path: "app/CMakeLists.txt"
      provides: "Multi-file firmware source registration"
      contains: "app/src"
  key_links:
    - from: "app/src/app/app_context.h"
      to: "app/src/network/network_state.h"
      via: "explicit typed composition"
      pattern: "network_(runtime_)?state"
---

<objective>
Define the Phase 1 module layout, shared interfaces, and future subsystem homes.

Purpose: Establish durable ownership boundaries before moving real runtime behavior out of `main.c`.
Output: New `app/src/` subsystem layout, typed shared headers, and build wiring prepared for multi-file firmware code.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-refactor/01-CONTEXT.md
@.planning/phases/01-foundation-refactor/01-RESEARCH.md
@app/CMakeLists.txt
@app/Kconfig
@app/src/main.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed app and networking contracts</name>
  <files>app/src/app/app_config.h, app/src/app/app_context.h, app/src/app/bootstrap.h, app/src/network/network_state.h, app/src/network/wifi_lifecycle.h, app/src/network/reachability.h</files>
  <action>Create the shared headers that will own immutable boot/config data, subsystem-owned networking state, and the bootstrap-facing networking APIs. Keep the contracts narrow and explicit: use real structs and function prototypes for the behavior Phase 1 will implement, and do not create a catch-all global header or a generic utils bucket.</action>
  <verify>test -f app/src/app/app_context.h && test -f app/src/app/bootstrap.h && test -f app/src/network/network_state.h && test -f app/src/network/wifi_lifecycle.h</verify>
  <done>Typed contracts exist for app config/context and networking lifecycle ownership, with no hidden cross-module global state requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Establish future subsystem homes without fake implementation</name>
  <files>app/src/recovery/recovery.h, app/src/panel/panel_http.h, app/src/panel/panel_auth.h, app/src/actions/actions.h, app/src/scheduler/scheduler.h, app/src/persistence/persistence.h, app/src/ota/ota.h, app/src/relay/relay.h</files>
  <action>Create minimal placeholder module boundaries for the later roadmap subsystems. These files should make ownership obvious and compile cleanly, but they must not invent speculative runtime behavior or dead scaffolding that pretends later phases are already implemented.</action>
  <verify>test -f app/src/recovery/recovery.h && test -f app/src/panel/panel_http.h && test -f app/src/relay/relay.h</verify>
  <done>Every later-phase subsystem has a clear long-term home in the tree, with lightweight placeholders instead of fake implementations.</done>
</task>

<task type="auto">
  <name>Task 3: Update build wiring for multi-file growth</name>
  <files>app/CMakeLists.txt</files>
  <action>Refactor the app build definition so it is ready for the new multi-file firmware layout and include paths while staying simple and Zephyr-native. Avoid adding unnecessary build abstraction, custom generation steps, or script-driven source discovery.</action>
  <verify>./scripts/build.sh && rg -n "target_sources|zephyr_library_sources|zephyr_include_directories|target_include_directories" app/CMakeLists.txt</verify>
  <done>`app/CMakeLists.txt` is ready to compile extracted source files and headers under the new subsystem layout.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] test -f app/src/app/app_context.h
- [ ] test -f app/src/network/network_state.h
- [ ] rg -n "app_context|network_(runtime_)?state|wifi_" app/src/app app/src/network
- [ ] ./scripts/build.sh
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The source tree exposes durable homes for the current and future subsystem responsibilities in Phase 1 scope
- Shared contracts are explicit typed interfaces, ready for runtime extraction in the next plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-refactor/01-01-SUMMARY.md`
</output>
