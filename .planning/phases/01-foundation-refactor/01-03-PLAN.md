---
phase: 01-foundation-refactor
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - README.md
  - scripts/common.sh
  - scripts/doctor.sh
  - scripts/validate.sh
autonomous: false
requirements: [PLAT-01, PLAT-02, PLAT-03]
user_setup: []
must_haves:
  truths:
    - "Developers can run a single validation entrypoint during refactor work to catch build regressions quickly."
    - "The repository clearly distinguishes automated build-first validation from final hardware sign-off."
    - "Phase completion requires an explicit hardware smoke confirmation of the ready-state markers."
  artifacts:
    - path: "scripts/validate.sh"
      provides: "Refactor-safe validation entrypoint"
      min_lines: 20
      contains: "build.sh"
    - path: "README.md"
      provides: "Documented build and hardware sign-off workflow"
      contains: "APP_READY"
  key_links:
    - from: "scripts/validate.sh"
      to: "scripts/build.sh"
      via: "delegated automated build check"
      pattern: "build\.sh"
    - from: "README.md"
      to: "scripts/validate.sh"
      via: "documented developer validation command"
      pattern: "scripts/validate\.sh"
---

<objective>
Add build-first validation scaffolding that protects the refactor without forcing hardware-only feedback on every iteration.

Purpose: Preserve fast feedback during Phase 1 while keeping the final real-device bring-up sign-off explicit and documented.
Output: A repo-owned validation entrypoint, clear developer guidance, and a blocking hardware sign-off checkpoint before the phase can be considered complete.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
@/Users/l/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-refactor/01-CONTEXT.md
@.planning/phases/01-foundation-refactor/01-RESEARCH.md
@README.md
@scripts/build.sh
@scripts/common.sh
@scripts/doctor.sh
@scripts/flash.sh
@scripts/console.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a validation entrypoint that reuses the build workflow</name>
  <files>scripts/validate.sh, scripts/common.sh</files>
  <action>Add a repo-owned validation script that runs the repeatable automated checks for Phase 1 by delegating to the existing build flow instead of duplicating it. Keep the fast path non-hardware by default. If you support a hardware mode, make it an explicit opt-in and keep the base command safe for frequent refactor feedback.</action>
  <verify>bash -n scripts/validate.sh && test -x scripts/validate.sh</verify>
  <done>`scripts/validate.sh` exists, follows the repo's shell conventions, and provides a single validation command for refactor work.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify build-first versus hardware sign-off guidance</name>
  <files>scripts/doctor.sh, README.md</files>
  <action>Make the repo guidance explicit about what is automated and what remains manual-only. If `scripts/doctor.sh` needs a minimal adjustment or messaging improvement to support that split, keep it small and purposeful. Document the expected ready-state smoke markers (`Wi-Fi connected`, `DHCP IPv4 address`, `Reachability check passed`, `APP_READY`) in `README.md`.</action>
  <verify>rg -n "validate\.sh|APP_READY|Reachability check passed|hardware" README.md scripts/doctor.sh</verify>
  <done>The repository clearly explains the automated validation path and the final manual hardware smoke procedure, including the expected success markers.</done>
</task>

<task type="auto">
  <name>Task 3: Run the build-first validation path and stage the hardware smoke gate</name>
  <files>scripts/validate.sh, README.md</files>
  <action>Run the new validation entrypoint and confirm it uses `scripts/build.sh` as the canonical automated build signal. Ensure the documented hardware smoke sequence is the final gate and that the eventual plan summary must record whether `./scripts/flash.sh` plus `./scripts/console.sh` reached the expected ready-state markers.</action>
  <verify>./scripts/validate.sh && rg -n "flash\.sh|console\.sh|APP_READY" README.md</verify>
  <done>The automated validation path works, and the hardware smoke instructions are staged as a required final sign-off rather than optional documentation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm hardware smoke on device</name>
  <files>README.md, scripts/flash.sh, scripts/console.sh</files>
  <action>Collect final device-level confirmation that the refactored firmware still reaches the expected ready state on hardware before Phase 1 can be considered complete.</action>
  <verify>Human checkpoint approved with all required ready-state markers observed, or the failing stage is recorded.</verify>
  <done>Hardware smoke sign-off is explicitly approved or blocked with a concrete failure note.</done>
  <what-built>Phase 1 validation flow and final hardware smoke gate</what-built>
  <how-to-verify>
    Using the refactored firmware build and the repo scripts, confirm the final real-device smoke sequence succeeds:
    1. Flash the image with `./scripts/flash.sh`
    2. Open the device console with `./scripts/console.sh`
    3. Verify the log reaches all of these markers: `Wi-Fi connected`, `DHCP IPv4 address: ...`, `Reachability check passed`, and `APP_READY`
    4. Confirm the Phase 1 summary records that hardware smoke passed (or records the failing stage if it did not)
  </how-to-verify>
  <resume-signal>Type "approved" once the hardware smoke gate is confirmed, or describe the failing stage</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bash -n scripts/validate.sh
- [ ] ./scripts/validate.sh
- [ ] rg -n "APP_READY|Reachability check passed|scripts/validate\.sh|flash\.sh|console\.sh" README.md
- [ ] Hardware smoke checkpoint approved with the required ready-state markers observed on device
- [ ] If `shellcheck` is available, run it on touched shell scripts without introducing new warnings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has a build-first validation path that does not require hardware on every iteration
- Final hardware smoke validation is a blocking sign-off gate, not optional documentation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-refactor/01-03-SUMMARY.md`
</output>
