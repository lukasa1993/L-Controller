---
phase: 01-foundation-refactor
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/CMakeLists.txt
  - app/src/main.c
  - app/src/app/bootstrap.c
  - app/src/app/bootstrap.h
  - app/src/network/network_state.h
  - app/src/network/wifi_lifecycle.c
  - app/src/network/wifi_lifecycle.h
  - app/src/network/reachability.c
  - app/src/network/reachability.h
autonomous: true
requirements: [PLAT-02, PLAT-03]
user_setup: []
must_haves:
  truths:
    - "`main.c` acts only as the firmware composition/bootstrap root and no longer owns subsystem business logic."
    - "Wi-Fi bring-up state and callback handling are owned by the networking subsystem through explicit types and APIs."
    - "The existing ready-path semantics still flow through connect, DHCP, reachability, and `APP_READY`."
  artifacts:
    - path: "app/src/app/bootstrap.c"
      provides: "Bootstrap orchestration entrypoint"
      min_lines: 30
      contains: "app_boot"
    - path: "app/src/network/wifi_lifecycle.c"
      provides: "Wi-Fi callback, connect, and wait lifecycle implementation"
      min_lines: 80
      contains: "NET_REQUEST_WIFI_CONNECT"
    - path: "app/src/network/reachability.c"
      provides: "Post-connect reachability probe"
      min_lines: 30
      contains: "getaddrinfo"
  key_links:
    - from: "app/src/main.c"
      to: "app/src/app/bootstrap.h"
      via: "composition-root boot call"
      pattern: "app_boot"
    - from: "app/src/app/bootstrap.c"
      to: "app/src/network/wifi_lifecycle.h"
      via: "bootstrap-owned Wi-Fi bring-up sequence"
      pattern: "wifi_"
    - from: "app/src/app/bootstrap.c"
      to: "app/src/network/reachability.h"
      via: "ready-state reachability gate"
      pattern: "reachability|run_reachability"
---

<objective>
Extract the existing runtime bring-up flow out of `main.c` into real bootstrap and networking modules.

Purpose: Satisfy the core Phase 1 success criteria by turning the monolith into subsystem-owned runtime code without changing the product behavior.
Output: `main.c` reduced to composition, plus real bootstrap/network implementation files that preserve the current ready-state flow.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-refactor/01-CONTEXT.md
@.planning/phases/01-foundation-refactor/01-RESEARCH.md
@app/CMakeLists.txt
@app/src/main.c
@app/Kconfig
@app/prj.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Wi-Fi lifecycle ownership into the networking subsystem</name>
  <files>app/src/network/network_state.h, app/src/network/wifi_lifecycle.c, app/src/network/wifi_lifecycle.h</files>
  <action>Extract callback registration, connection/disconnect/DHCP handlers, timeout logic, and connect/wait sequencing into a networking module with subsystem-owned state. Preserve the current log/error behavior where it encodes real readiness or failure semantics, and keep mutable connection state out of `main.c`.</action>
  <verify>./scripts/build.sh && rg -n "handle_wifi_connect_result|NET_EVENT_IPV4_DHCP_BOUND|NET_REQUEST_WIFI_CONNECT" app/src/network/wifi_lifecycle.c && rg -n "network_(runtime_)?state|app_context" app/src/network/wifi_lifecycle.c app/src/network/network_state.h</verify>
  <done>The networking subsystem owns Wi-Fi bring-up state and exposes narrow bootstrap-facing APIs instead of requiring hidden cross-file globals.</done>
</task>

<task type="auto">
  <name>Task 2: Extract reachability and bootstrap sequencing</name>
  <files>app/src/app/bootstrap.c, app/src/app/bootstrap.h, app/src/network/reachability.c, app/src/network/reachability.h</files>
  <action>Move the post-connect reachability probe into its own module and implement a bootstrap entrypoint that sequences interface discovery, callback registration, Wi-Fi readiness, connection, DHCP wait, reachability, and readiness logging. Keep direct networking behavior out of `main.c` and avoid thin wrappers that leave ownership ambiguous.</action>
  <verify>./scripts/build.sh && rg -n "getaddrinfo|APP_READY|app_boot" app/src/app/bootstrap.c app/src/network/reachability.c && rg -n "app_context|network_(runtime_)?state" app/src/app/bootstrap.c app/src/network/reachability.c</verify>
  <done>Bootstrap owns orchestration, reachability owns the TCP probe, and the overall ready path is represented by subsystem entrypoints instead of a monolithic `main.c`.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce `main.c` to composition root and restore the build</name>
  <files>app/src/main.c, app/CMakeLists.txt</files>
  <action>Rewrite `main.c` so it performs only top-level setup/composition and invokes the bootstrap entrypoint. Update the build manifest to compile the extracted sources. Do not leave callback bodies, semaphores, timeout logic, direct `net_mgmt` usage, or reachability logic inside `main.c` after the refactor.</action>
  <verify>./scripts/build.sh && ! rg -n "K_SEM_DEFINE|net_mgmt\(|handle_wifi_|handle_ipv4_dhcp_bound|getaddrinfo|socket\(" app/src/main.c</verify>
  <done>`main.c` is composition-only, the extracted files are built through the existing script, and the former runtime logic no longer lives in the composition root.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] ! rg -n "K_SEM_DEFINE|net_mgmt\(|handle_wifi_|handle_ipv4_dhcp_bound|getaddrinfo|socket\(" app/src/main.c
- [ ] rg -n "app_boot|bootstrap" app/src/main.c app/src/app/bootstrap.c
- [ ] rg -n "app_context|network_(runtime_)?state" app/src/app/bootstrap.c app/src/network/wifi_lifecycle.c app/src/network/reachability.c
- [ ] rg -n "NET_REQUEST_WIFI_CONNECT|NET_EVENT_IPV4_DHCP_BOUND" app/src/network/wifi_lifecycle.c
- [ ] rg -n "getaddrinfo|connect\(" app/src/network/reachability.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `main.c` no longer contains subsystem business logic or hidden runtime state machinery
- The bootstrap-to-network-to-reachability flow still represents the existing ready-state behavior through explicit interfaces and typed contracts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-refactor/01-02-SUMMARY.md`
</output>
