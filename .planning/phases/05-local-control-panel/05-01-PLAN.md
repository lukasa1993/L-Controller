---
phase: 05-local-control-panel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/CMakeLists.txt
  - app/Kconfig
  - app/prj.conf
  - app/sections-rom.ld
  - app/src/app/app_config.h
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/panel/panel_http.h
  - app/src/panel/panel_http.c
  - app/src/panel/assets/index.html
  - app/src/panel/assets/main.js
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, PANEL-01, PANEL-02, PANEL-03]
user_setup: []
must_haves:
  truths:
    - "Phase 5 starts with a real Zephyr HTTP service boundary that serves only the public panel shell and embedded authored assets over local HTTP."
    - "Panel markup and JavaScript live as authored asset files compressed into the firmware image at build time, not as C-generated HTML strings or a mounted filesystem."
    - "This plan stays transport- and asset-focused: no login/session routes, no protected operator APIs, and no relay/scheduler/update control surfaces are introduced yet."
  artifacts:
    - path: "app/src/panel/panel_http.c"
      provides: "HTTP service shell with startup logic and public static resource registration for the panel"
      min_lines: 140
      contains: "HTTP_RESOURCE_TYPE_STATIC"
    - path: "app/CMakeLists.txt"
      provides: "Panel build wiring for panel sources, gzip asset generation, and linker section registration"
      contains: "generate_inc_file_for_target"
    - path: "app/sections-rom.ld"
      provides: "Dedicated iterable linker section for panel HTTP resource descriptors"
      contains: "http_resource_desc_panel_http_service"
    - path: "app/src/panel/assets/index.html"
      provides: "Authored public shell page that loads the local panel JavaScript and Tailwind Play CDN"
      min_lines: 20
      contains: "cdn.tailwindcss.com"
  key_links:
    - from: "app/CMakeLists.txt"
      to: "app/src/panel/assets/index.html"
      via: "build gzips the authored panel assets into embedded HTTP static resources"
      pattern: 'generate_inc_file_for_target|index\.html|main\.js'
    - from: "app/src/panel/panel_http.c"
      to: "app/sections-rom.ld"
      via: "panel resources register into the dedicated HTTP iterable linker section"
      pattern: 'http_resource_desc_panel_http_service'
    - from: "app/src/app/bootstrap.c"
      to: "app/src/panel/panel_http.h"
      via: "boot composes and starts the public panel shell before returning APP_READY"
      pattern: 'panel_http_server_init'
---

<objective>
Build the HTTP service shell, embedded static asset pipeline, and linker/build wiring for Phase 5.

Purpose: Create the smallest Zephyr-native panel foundation that can serve authored local assets from firmware, fit the existing `app_context`/`bootstrap` ownership model, and leave all auth plus protected status behavior for later plans.
Output: A compiled `panel_http` service, embedded `index.html`/`main.js` assets, linker support for HTTP resources, and boot wiring that makes the public shell available over local HTTP.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-local-control-panel/05-CONTEXT.md
@.planning/phases/05-local-control-panel/05-RESEARCH.md
@.planning/phases/05-local-control-panel/05-VALIDATION.md
@app/CMakeLists.txt
@app/Kconfig
@app/prj.conf
@app/src/app/app_config.h
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/panel/panel_http.h
@app/src/panel/panel_auth.h
@app/src/persistence/persistence.h
@app/src/persistence/persistence_types.h
@app/src/network/network_state.h
@app/src/recovery/recovery.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the panel HTTP build surface, linker section, and local-LAN config boundary</name>
  <files>app/CMakeLists.txt, app/Kconfig, app/prj.conf, app/sections-rom.ld, app/src/app/app_config.h, app/src/panel/panel_http.h, app/src/panel/panel_http.c</files>
  <action>Add the Zephyr HTTP server integration for the panel shell: compile `panel_http.c`, register the panel HTTP iterable resource section, and introduce only the small transport/config surface this wave needs (`APP_PANEL_PORT` plus the required HTTP server transport options and captured-header support). Keep the service on plain local HTTP, avoid TLS/websocket/filesystem features, and do not add auth/session or protected API routes in this plan.</action>
  <verify>
    <automated>test -f app/src/panel/panel_http.c &amp;&amp; test -f app/sections-rom.ld &amp;&amp; rg -n "CONFIG_HTTP_SERVER|CONFIG_HTTP_SERVER_CAPTURE_HEADERS|APP_PANEL_PORT" app/Kconfig app/prj.conf app/src/app/app_config.h &amp;&amp; rg -n "panel_http\\.c|zephyr_linker_sources|http_resource_desc_panel_http_service" app/CMakeLists.txt app/sections-rom.ld &amp;&amp; ! rg -n "/api/auth|/api/status|/api/(relay|schedule|update)" app/src/panel/panel_http.c</automated>
  </verify>
  <done>The repo builds a real panel HTTP shell boundary with explicit local-LAN configuration and no premature protected-route scope.</done>
</task>

<task type="auto">
  <name>Task 2: Embed authored HTML and JavaScript assets with a gzip build pipeline</name>
  <files>app/CMakeLists.txt, app/src/panel/assets/index.html, app/src/panel/assets/main.js, app/src/panel/panel_http.c</files>
  <action>Create authored `index.html` and `main.js` assets under `app/src/panel/assets/`, gzip them at build time with `generate_inc_file_for_target(... --gzip)`, and register them as static HTTP resources for `/` and `/main.js`. Include the Tailwind Play CDN hook in the HTML shell, but keep the asset flow functional even before auth/status behavior exists. Do not move markup generation into C or introduce a filesystem dependency.</action>
  <verify>
    <automated>test -f app/src/panel/assets/index.html &amp;&amp; test -f app/src/panel/assets/main.js &amp;&amp; rg -n "generate_inc_file_for_target|--gzip|index\\.html|main\\.js" app/CMakeLists.txt &amp;&amp; rg -n "cdn\\.tailwindcss\\.com|main\\.js" app/src/panel/assets/index.html &amp;&amp; ! rg -n "localStorage|sessionStorage" app/src/panel/assets/main.js</automated>
  </verify>
  <done>The panel serves real embedded frontend assets from the firmware image instead of C-generated markup.</done>
</task>

<task type="auto">
  <name>Task 3: Compose the panel shell into app ownership and boot readiness</name>
  <files>app/src/app/app_context.h, app/src/app/bootstrap.c, app/src/app/app_config.h, app/src/panel/panel_http.h, app/src/panel/panel_http.c</files>
  <action>Extend `app_context` and app config to own the panel HTTP service by value, initialize it from `app_boot()`, and start the public shell before boot returns. Keep the shell available when the device is locally reachable even if upstream internet is degraded, and do not disturb the established persistence, recovery, or network readiness sequencing beyond the minimal composition needed for the public panel shell.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "panel_http|APP_PANEL_PORT" app/src/app/app_context.h app/src/app/app_config.h app/src/app/bootstrap.c app/src/panel/panel_http.c app/src/panel/panel_http.h &amp;&amp; rg -n "APP_READY|NETWORK_CONNECTIVITY_LAN_UP_UPSTREAM_DEGRADED" app/src/app/bootstrap.c app/src/network/network_state.h</automated>
  </verify>
  <done>The public panel shell is part of normal app composition and can come up without waiting for future auth or dashboard features.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] test -f app/src/panel/panel_http.c &amp;&amp; test -f app/sections-rom.ld &amp;&amp; rg -n "CONFIG_HTTP_SERVER|CONFIG_HTTP_SERVER_CAPTURE_HEADERS|APP_PANEL_PORT" app/Kconfig app/prj.conf app/src/app/app_config.h
- [ ] test -f app/src/panel/assets/index.html &amp;&amp; test -f app/src/panel/assets/main.js &amp;&amp; rg -n "generate_inc_file_for_target|--gzip|index\\.html|main\\.js" app/CMakeLists.txt
- [ ] rg -n "panel_http|APP_PANEL_PORT" app/src/app/app_context.h app/src/app/app_config.h app/src/app/bootstrap.c app/src/panel/panel_http.c app/src/panel/panel_http.h
- [ ] ! rg -n "/api/auth|/api/status|/api/(relay|schedule|update)" app/src/panel/panel_http.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a real Zephyr HTTP panel shell and linker/resource pipeline rather than placeholder panel headers only
- Authored `index.html` and `main.js` assets are embedded at build time and served locally without C-generated markup
- Phase 5 remains read-only for relay, scheduler, and update surfaces because this plan exposes only the public shell assets

</success_criteria>

<output>
After completion, create `.planning/phases/05-local-control-panel/05-01-SUMMARY.md`
</output>
