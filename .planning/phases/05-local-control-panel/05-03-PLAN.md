---
phase: 05-local-control-panel
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/src/panel/assets/index.html
  - app/src/panel/assets/main.js
  - README.md
  - scripts/common.sh
  - scripts/validate.sh
  - app/wifi.secrets.conf.example
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, PANEL-01, PANEL-02, PANEL-03]
user_setup: []
must_haves:
  truths:
    - "The authored frontend assets present a login-first dashboard that rehydrates session state from the server on load, keeps refresh/navigation predictable, and never stores auth state in browser-local persistence."
    - "The dashboard renders only the aggregate `/api/status` data plus explicit session UX, and relay/scheduler/update surfaces remain status-only or placeholder-only throughout Phase 5."
    - "Repo validation stays build-first for automation while Phase 5 approval remains blocked on a documented browser/curl/device sign-off that covers auth flow, session behavior, degraded styling, and read-only surface checks."
  artifacts:
    - path: "app/src/panel/assets/index.html"
      provides: "Authored login/dashboard shell with Tailwind Play CDN integration and graceful degraded-state messaging"
      min_lines: 40
      contains: "dashboard"
    - path: "app/src/panel/assets/main.js"
      provides: "Frontend session bootstrap, login/logout flow, and aggregate status rendering for the local panel"
      min_lines: 160
      contains: "fetch('/api/status')"
    - path: "scripts/validate.sh"
      provides: "Phase 5 automated validation entrypoint that preserves build-first feedback and points to the blocking manual sign-off"
      contains: "Phase 5"
    - path: "app/wifi.secrets.conf.example"
      provides: "Documented admin credential placeholders required to reproduce panel login behavior"
      contains: "CONFIG_APP_ADMIN_USERNAME"
  key_links:
    - from: "app/src/panel/assets/main.js"
      to: "app/src/panel/panel_http.c"
      via: "frontend consumes only the session and aggregate status endpoints exposed by the panel service"
      pattern: '/api/auth/session|/api/status'
    - from: "README.md"
      to: "scripts/flash.sh"
      via: "documentation records the real-device panel validation flow for final approval"
      pattern: 'flash\.sh|console\.sh'
    - from: "scripts/validate.sh"
      to: "scripts/build.sh"
      via: "automated validation remains build-first before the manual browser and device gate"
      pattern: 'build\.sh'
    - from: "app/wifi.secrets.conf.example"
      to: "app/Kconfig"
      via: "setup guidance exposes the admin credential knobs required for reproducible login testing"
      pattern: 'CONFIG_APP_ADMIN_(USERNAME|PASSWORD)'
---

<objective>
Ship the initial operator dashboard and session UX, then stage the Phase 5 validation and documentation flow.

Purpose: Turn the protected auth and status APIs from Plans 1-2 into a usable local operator experience that renders meaningful device state, keeps browser session behavior predictable, and documents the blocking real-device sign-off required for completion.
Output: Final Phase 5 frontend assets, documented validation/setup guidance, and a blocking browser/curl/device checkpoint for auth, session, and dashboard behavior.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
@/Users/l/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-local-control-panel/05-CONTEXT.md
@.planning/phases/05-local-control-panel/05-RESEARCH.md
@.planning/phases/05-local-control-panel/05-VALIDATION.md
@README.md
@scripts/build.sh
@scripts/common.sh
@scripts/flash.sh
@scripts/console.sh
@scripts/validate.sh
@app/wifi.secrets.conf.example
@app/src/panel/panel_http.h
@app/src/panel/panel_auth.h
@app/src/persistence/persistence_types.h
@app/src/network/network_state.h
@app/src/recovery/recovery.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the login-first shell and server-truth session bootstrap</name>
  <files>app/src/panel/assets/index.html, app/src/panel/assets/main.js</files>
  <action>Turn the public shell into the Phase 5 operator UI: render a login-first page, call `GET /api/auth/session` on load, wire `POST /api/auth/login` and `POST /api/auth/logout`, and keep refresh/navigation behavior predictable by trusting only the server-issued cookie/session state. Include the Tailwind Play CDN in the authored HTML, but avoid local token storage, generated markup in C, or browser-persisted credentials.</action>
  <verify>
    <automated>rg -n "cdn\\.tailwindcss\\.com|/api/auth/session|/api/auth/login|/api/auth/logout|login|logout|dashboard" app/src/panel/assets/index.html app/src/panel/assets/main.js &amp;&amp; ! rg -n "localStorage|sessionStorage|Authorization:" app/src/panel/assets/main.js</automated>
  </verify>
  <done>The local panel opens into a predictable login-first experience that rehydrates session state from the server instead of the browser.</done>
</task>

<task type="auto">
  <name>Task 2: Render aggregate status cards and keep future control surfaces read-only</name>
  <files>app/src/panel/assets/index.html, app/src/panel/assets/main.js</files>
  <action>Consume the protected aggregate `/api/status` payload to render device, network, relay, scheduler, and update cards. Surface degraded-but-local states clearly (including `LAN_UP_UPSTREAM_DEGRADED` behavior), and present relay, scheduler, and update as status-only or explicit Phase 6-8 placeholders with no mutating fetches or control affordances in this phase.</action>
  <verify>
    <automated>rg -n "/api/status|LAN_UP_UPSTREAM_DEGRADED|NETWORK_CONNECTIVITY_LAN_UP_UPSTREAM_DEGRADED|relay|scheduler|update|placeholder|implemented" app/src/panel/assets/index.html app/src/panel/assets/main.js &amp;&amp; ! rg -n "/api/(relay|schedule|update)" app/src/panel/assets/main.js</automated>
  </verify>
  <done>The dashboard shows current state from one read-only API and leaves relay, scheduler, and update behavior clearly deferred to later phases.</done>
</task>

<task type="auto">
  <name>Task 3: Update validation/docs and document reproducible admin setup</name>
  <files>README.md, scripts/common.sh, scripts/validate.sh, app/wifi.secrets.conf.example</files>
  <action>Extend the repo documentation and validation scripts to describe the Phase 5 build-first automated path plus the blocking browser/curl/device sign-off. Add Phase 5 checklist/helper output in `scripts/common.sh`, update `scripts/validate.sh` messaging, and document `CONFIG_APP_ADMIN_USERNAME` plus `CONFIG_APP_ADMIN_PASSWORD` in `app/wifi.secrets.conf.example` so login testing is reproducible from a clean checkout.</action>
  <verify>
    <automated>bash -n scripts/validate.sh scripts/common.sh scripts/build.sh &amp;&amp; rg -n "Phase 5|panel|login|logout|cookie|status|browser|curl|CONFIG_APP_ADMIN_USERNAME|CONFIG_APP_ADMIN_PASSWORD" README.md scripts/validate.sh scripts/common.sh app/wifi.secrets.conf.example</automated>
  </verify>
  <done>The repo explains how to build, configure, and sign off the Phase 5 panel without inventing a new automated test harness.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm auth, session, and dashboard behavior on a real device and browser</name>
  <files>README.md, scripts/flash.sh, scripts/console.sh, scripts/validate.sh</files>
  <action>Collect final browser/curl/device confirmation that Phase 5 auth/session behavior and the read-only dashboard are correct on the local LAN before the phase is considered complete.</action>
  <verify>Human checkpoint approved with auth denial, login success, refresh/navigation persistence, logout revocation, cooldown recovery, concurrent-browser sessions, reboot-bounded invalidation, degraded-styling resilience, and read-only relay/scheduler/update surfaces recorded.</verify>
  <done>Phase 5 browser and device validation is explicitly approved or blocked with scenario-level notes.</done>
  <what-built>Phase 5 login/dashboard UX plus documented browser, curl, and device validation flow</what-built>
  <how-to-verify>
    Using the built firmware, the repo scripts, and one or more browsers on the same LAN, confirm the Phase 5 panel behaves correctly:
    1. Run `./scripts/validate.sh` and keep the automated build path green.
    2. Flash the image with `./scripts/flash.sh`.
    3. Open the console with `./scripts/console.sh` and determine the device IP from the boot log.
    4. Verify unauthenticated access is denied with `curl -i http://&lt;device-ip&gt;/api/status`.
    5. Log in with `curl -i -c cookie.txt -H 'Content-Type: application/json' -d '{"username":"...","password":"..."}' http://&lt;device-ip&gt;/api/auth/login` and confirm a session cookie is returned.
    6. Verify protected access works with `curl -i -b cookie.txt http://&lt;device-ip&gt;/api/status`.
    7. Open the panel in a browser, log in, refresh, and navigate within the Phase 5 views to confirm the session remains active until logout.
    8. Trigger repeated wrong-password attempts until the cooldown appears, wait for the cooldown window to expire, and confirm a valid login succeeds again.
    9. Log in from a second browser or private session and confirm concurrent sessions remain usable without evicting each other.
    10. Reboot the device, then confirm the old browser session can no longer reach protected routes until a fresh login occurs.
    11. Inspect the dashboard during normal and degraded-local conditions, confirm local status still renders if Tailwind CDN styling is reduced, and confirm relay, scheduler, and update sections remain read-only or placeholder-only.
    12. Record which auth denial, login, refresh/navigation, logout, cooldown, concurrent-session, reboot-invalidates-session, degraded-styling, and read-only-surface scenarios passed or failed in the Phase 5 summary.
  </how-to-verify>
  <resume-signal>Type "approved" once the Phase 5 browser and device scenarios are confirmed, or describe the failing scenario</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/validate.sh
- [ ] bash -n scripts/validate.sh scripts/common.sh scripts/build.sh
- [ ] rg -n "cdn\.tailwindcss\.com|/api/auth/session|/api/status|login|logout|placeholder" app/src/panel/assets/index.html app/src/panel/assets/main.js
- [ ] rg -n "Phase 5|panel|login|logout|cookie|status|browser|curl|CONFIG_APP_ADMIN_USERNAME|CONFIG_APP_ADMIN_PASSWORD" README.md scripts/validate.sh scripts/common.sh app/wifi.secrets.conf.example
- [ ] Human checkpoint approved with auth/session/status browser, curl, and device scenarios recorded
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The authored frontend keeps login/logout/session UX predictable without client-stored auth tokens or C-generated UI markup
- The dashboard renders current device, Wi-Fi, relay, scheduler, and update status from the aggregate API while keeping relay, scheduler, and update surfaces read-only in Phase 5
- Repo docs and validation scripts clearly separate the automated build gate from the blocking real-device panel sign-off and document the admin credential setup needed to reproduce login behavior

</success_criteria>

<output>
After completion, create `.planning/phases/05-local-control-panel/05-03-SUMMARY.md`
</output>
