---
phase: 05-local-control-panel
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/Kconfig
  - app/prj.conf
  - app/src/app/app_config.h
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/panel/panel_auth.h
  - app/src/panel/panel_auth.c
  - app/src/panel/panel_http.c
  - app/src/panel/panel_status.h
  - app/src/panel/panel_status.c
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, PANEL-01, PANEL-02, PANEL-03]
user_setup: []
must_haves:
  truths:
    - "Authentication uses the persisted single-operator credential snapshot plus cookie-backed RAM sessions, with concurrent logins allowed and reboot-bounded invalidation by design."
    - "Protected routing covers `/api/auth/logout`, `/api/auth/session`, and `/api/status`; unauthenticated callers receive a clean denial and stale session cookies are cleared rather than trusted."
    - "The only new operator data API is a read-only aggregate `/api/status` snapshot; relay, scheduler, and update surfaces stay read-only and Phase 5 does not introduce mutating control routes."
  artifacts:
    - path: "app/src/panel/panel_auth.c"
      provides: "Panel auth service with credential validation, cooldown bookkeeping, and RAM session management"
      min_lines: 180
      contains: "sys_csrand_get"
    - path: "app/src/panel/panel_status.c"
      provides: "Read-only status snapshot builder for device, network, recovery, relay, scheduler, and update panels"
      min_lines: 140
      contains: "network_supervisor_get_status"
    - path: "app/src/panel/panel_http.c"
      provides: "Exact-route login/logout/session/status HTTP callbacks with cookie-based auth gating"
      min_lines: 220
      contains: "/api/auth/login"
    - path: "app/src/app/app_config.h"
      provides: "Panel auth/session config surface for port-adjacent runtime knobs"
      contains: "struct app_panel_config"
  key_links:
    - from: "app/src/app/bootstrap.c"
      to: "app/src/panel/panel_auth.h"
      via: "boot initializes panel auth only after persisted credentials are loaded"
      pattern: 'panel_auth_service_init'
    - from: "app/src/panel/panel_http.c"
      to: "app/src/panel/panel_auth.h"
      via: "HTTP callbacks delegate login, session lookup, and cookie validation into the auth service"
      pattern: 'panel_auth_'
    - from: "app/src/panel/panel_status.c"
      to: "app/src/network/network_supervisor.h"
      via: "status shaping reuses the existing network summary contract instead of raw runtime flags"
      pattern: 'network_supervisor_get_status'
    - from: "app/src/panel/panel_status.c"
      to: "app/src/recovery/recovery.h"
      via: "device status includes the preserved recovery-reset cause through the shared recovery contract"
      pattern: 'recovery_manager_last_reset_cause'
---

<objective>
Implement single-user auth, cookie-backed RAM sessions, and protected read-only API routing for Phase 5.

Purpose: Turn the public HTTP shell from Plan 1 into a real operator boundary that validates the persisted single-user credential, preserves predictable browser sessions until explicit logout, and exposes only protected read-only status data.
Output: A `panel_auth` service, exact login/logout/session routes, and a protected aggregate `/api/status` endpoint that feeds the later dashboard without leaking future control scope.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-local-control-panel/05-CONTEXT.md
@.planning/phases/05-local-control-panel/05-RESEARCH.md
@.planning/phases/05-local-control-panel/05-VALIDATION.md
@app/Kconfig
@app/prj.conf
@app/src/app/app_config.h
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/panel/panel_http.h
@app/src/panel/panel_auth.h
@app/src/persistence/persistence.h
@app/src/persistence/persistence_types.h
@app/src/network/network_state.h
@app/src/network/network_supervisor.h
@app/src/recovery/recovery.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the panel auth service and reboot-bounded RAM session policy</name>
  <files>app/Kconfig, app/src/app/app_config.h, app/src/app/app_context.h, app/src/app/bootstrap.c, app/src/panel/panel_auth.h, app/src/panel/panel_auth.c</files>
  <action>Add the Phase 5 auth/session config surface (`APP_PANEL_MAX_SESSIONS`, `APP_PANEL_LOGIN_FAILURE_LIMIT`, `APP_PANEL_LOGIN_COOLDOWN_MS`), compose `panel_auth` into `app_context`, initialize it after persistence load, and implement a fixed-size RAM session table guarded by a mutex. Validate credentials against `app_context.persisted_config.auth`, allow concurrent active sessions, clear all sessions on boot, and enforce a short wrong-password cooldown without inventing multi-user, rotation, takeover, or persistent-token behavior.</action>
  <verify>
    <automated>test -f app/src/panel/panel_auth.c &amp;&amp; rg -n "APP_PANEL_MAX_SESSIONS|APP_PANEL_LOGIN_FAILURE_LIMIT|APP_PANEL_LOGIN_COOLDOWN_MS|persisted_config\\.auth|sys_csrand_get|k_mutex|cooldown|session" app/Kconfig app/src/app/app_config.h app/src/app/app_context.h app/src/app/bootstrap.c app/src/panel/panel_auth.c app/src/panel/panel_auth.h</automated>
  </verify>
  <done>The firmware owns a simple, predictable single-user auth service with RAM-only sessions and bounded wrong-password cooldown behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add exact auth routes, cookie handling, and protected-route gating</name>
  <files>app/prj.conf, app/src/panel/panel_http.c, app/src/panel/panel_auth.c, app/src/panel/panel_auth.h</files>
  <action>Enable the small HTTP/JSON configuration needed for auth callbacks, then add exact-path handlers for `POST /api/auth/login`, `POST /api/auth/logout`, and `GET /api/auth/session`. Capture the `Cookie` header, parse the login JSON body only once the full request has arrived, issue short `sid` cookies with `Path=/`, `HttpOnly`, and `SameSite=Strict`, clear stale cookies on `401` or logout, and keep the callback model short-lived and stateless. In Phase 5, `AUTH-02` is satisfied by protecting the implemented operator routes while leaving control, configuration, scheduling, and update endpoints intentionally unavailable rather than partially exposed. Do not add wildcard resources, TLS, websockets, or any relay/schedule/update/config control routes.</action>
  <verify>
    <automated>rg -n "CONFIG_JSON_LIBRARY|CONFIG_HTTP_SERVER_MAX_CLIENTS|CONFIG_HTTP_SERVER_MAX_HEADER_LEN|CONFIG_NET_MAX_CONTEXTS|CONFIG_NET_MAX_CONN" app/prj.conf &amp;&amp; rg -n "/api/auth/login|/api/auth/logout|/api/auth/session|Set-Cookie|Cookie|Path=/|SameSite=Strict|HttpOnly|HTTP_SERVER_REGISTER_HEADER_CAPTURE|401" app/src/panel/panel_http.c app/src/panel/panel_auth.c app/src/panel/panel_auth.h &amp;&amp; ! rg -n "/api/(relay|schedule|update|config)" app/src/panel/panel_http.c</automated>
  </verify>
  <done>The panel exposes only the exact auth/session routes Phase 5 needs, and protected routing trusts server-issued RAM sessions instead of browser-held credentials.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the protected aggregate status API and keep future surfaces read-only</name>
  <files>app/src/panel/panel_http.c, app/src/panel/panel_status.h, app/src/panel/panel_status.c</files>
  <action>Create `panel_status` as a read-only translation layer and expose one protected `GET /api/status` endpoint. Build the response from existing app-owned state using `network_supervisor_get_status()`, `recovery_manager_last_reset_cause()`, and `persisted_config` so the payload covers device, network, recovery, relay, scheduler, and update sections without inventing fake control state. Report relay, scheduler, and update as status-only or explicit placeholders, and do not add any mutating API surface for those domains in Phase 5.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; test -f app/src/panel/panel_status.c &amp;&amp; rg -n "/api/status|network_supervisor_get_status|recovery_manager_last_reset_cause|persisted_config|relay|schedule|update|implemented" app/src/panel/panel_http.c app/src/panel/panel_status.c app/src/panel/panel_status.h &amp;&amp; ! rg -n "/api/(relay|schedule|update)" app/src/panel/panel_http.c</automated>
  </verify>
  <done>The panel has one protected read-only status API that future UI waves can render without broadening Phase 5 into control behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] test -f app/src/panel/panel_auth.c &amp;&amp; rg -n "APP_PANEL_MAX_SESSIONS|APP_PANEL_LOGIN_FAILURE_LIMIT|APP_PANEL_LOGIN_COOLDOWN_MS|persisted_config\.auth|sys_csrand_get|k_mutex|cooldown|session" app/Kconfig app/src/app/app_config.h app/src/app/app_context.h app/src/app/bootstrap.c app/src/panel/panel_auth.c app/src/panel/panel_auth.h
- [ ] rg -n "CONFIG_JSON_LIBRARY|CONFIG_HTTP_SERVER_MAX_CLIENTS|CONFIG_HTTP_SERVER_MAX_HEADER_LEN|CONFIG_NET_MAX_CONTEXTS|CONFIG_NET_MAX_CONN" app/prj.conf
- [ ] rg -n "/api/auth/login|/api/auth/logout|/api/auth/session|Set-Cookie|Cookie|Path=/|SameSite=Strict|HttpOnly|HTTP_SERVER_REGISTER_HEADER_CAPTURE|401" app/src/panel/panel_http.c app/src/panel/panel_auth.c app/src/panel/panel_auth.h
- [ ] test -f app/src/panel/panel_status.c &amp;&amp; rg -n "/api/status|network_supervisor_get_status|recovery_manager_last_reset_cause|persisted_config|relay|schedule|update|implemented" app/src/panel/panel_http.c app/src/panel/panel_status.c app/src/panel/panel_status.h
- [ ] ! rg -n "/api/(relay|schedule|update|config)" app/src/panel/panel_http.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Login, logout, and session-check routes work from the persisted single-user credential source with RAM-only cookie sessions
- Unauthenticated callers cannot reach protected operator status data and stale cookies are cleared predictably
- `/api/status` becomes the only Phase 5 operator data API and keeps relay, scheduler, and update surfaces read-only for later phases

</success_criteria>

<output>
After completion, create `.planning/phases/05-local-control-panel/05-02-SUMMARY.md`
</output>
