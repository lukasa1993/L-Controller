# Deferred Items

- 2026-03-09: `./scripts/build.sh` emits existing MBEDTLS PSA RNG Kconfig warnings from the current NCS configuration. The build still completes successfully, and the warnings pre-date plan `05-01`, so they remain out of scope for this execution.
