---
phase: 04-persistent-configuration
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/persistence/persistence.h
  - app/src/persistence/persistence.c
  - app/src/persistence/persistence_types.h
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
user_setup: []
must_haves:
  truths:
    - "Auth, relay/action, and schedule configuration each have typed section-level load/save APIs instead of one monolithic whole-device snapshot writer."
    - "Missing or invalid auth data reseeds the fixed single-operator credentials from the configured source, while clean-device action and schedule defaults remain intentionally blank and safe."
    - "Relay last desired state, relay reboot policy, and schedule-to-action references are all represented in typed repository contracts that validate data before it becomes durable."
  artifacts:
    - path: "app/src/persistence/persistence.c"
      provides: "Working section repositories for auth, relay/action, and schedule persistence"
      min_lines: 180
      contains: "persistence_store_"
    - path: "app/src/persistence/persistence_types.h"
      provides: "Typed persisted records for auth, relay/action, and schedules plus request-validation shapes"
      min_lines: 120
      contains: "struct persisted_auth"
    - path: "app/src/app/bootstrap.c"
      provides: "Boot-time resolved snapshot loading that later phases can consume"
      contains: "persisted_config"
  key_links:
    - from: "app/src/app/bootstrap.c"
      to: "app/src/persistence/persistence.h"
      via: "boot loads the resolved persisted snapshot and reports section outcomes"
      pattern: 'persistence_store_(load|save)|persisted_config'
    - from: "app/src/app/app_context.h"
      to: "app/src/persistence/persistence_types.h"
      via: "app context stores the resolved typed persistence snapshot by value"
      pattern: 'persisted_(auth|action|relay|schedule|config)'
    - from: "app/src/persistence/persistence.c"
      to: "app/src/persistence/persistence_types.h"
      via: "repository logic validates and persists typed section records"
      pattern: 'persisted_|schedule|action|relay|auth'
---

<objective>
Implement typed repositories for auth, action/relay, and schedule configuration.

Purpose: Turn the schema boundary from Plan 1 into real section-level load/save behavior that can seed safe defaults, preserve relay intent plus reboot policy, and reject invalid references before data becomes durable.
Output: Working repository APIs and a boot-loaded persisted snapshot for auth, relay/action, and schedules.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-persistent-configuration/04-CONTEXT.md
@.planning/phases/04-persistent-configuration/04-RESEARCH.md
@.planning/phases/04-persistent-configuration/04-VALIDATION.md
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/persistence/persistence.h
@app/src/persistence/persistence.c
@app/src/persistence/persistence_types.h
@app/src/panel/panel_auth.h
@app/src/actions/actions.h
@app/src/relay/relay.h
@app/src/scheduler/scheduler.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the auth repository and known-credential reseed path</name>
  <files>app/src/persistence/persistence.h, app/src/persistence/persistence.c, app/src/persistence/persistence_types.h, app/src/app/bootstrap.c</files>
  <action>Implement the auth section read/save helpers and resolved auth snapshot. When auth data is missing or invalid, reseed the fixed single-operator credentials from the build-time config source and report that outcome explicitly. Keep runtime account-management UX and session behavior out of scope for this phase.</action>
  <verify>
    <automated>rg -n "APP_ADMIN_USERNAME|APP_ADMIN_PASSWORD|auth|reseed|credentials" app/src/persistence/persistence.h app/src/persistence/persistence.c app/src/persistence/persistence_types.h app/src/app/bootstrap.c</automated>
  </verify>
  <done>Auth configuration loads predictably and missing or invalid auth data falls back to the known configured credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Implement relay/action and schedule repositories with staged validation</name>
  <files>app/src/persistence/persistence.h, app/src/persistence/persistence.c, app/src/persistence/persistence_types.h</files>
  <action>Add typed repositories for the action catalog, relay configuration, and schedule records. Persist relay last desired state and the configurable reboot policy, allow disabled-but-valid action and schedule entries to remain durable, and validate schedule-to-action references before any caller request is committed. Keep the write model section-scoped rather than rewriting a single whole-device blob for every edit.</action>
  <verify>
    <automated>rg -n "last_desired|reboot_policy|action|schedule|disabled|reference|validate" app/src/persistence/persistence.h app/src/persistence/persistence.c app/src/persistence/persistence_types.h</automated>
  </verify>
  <done>Action, relay, and schedule configuration each have typed section-level load/save paths with safe defaults and self-consistent references.</done>
</task>

<task type="auto">
  <name>Task 3: Load the resolved snapshot into app context and prove the build</name>
  <files>app/src/app/app_context.h, app/src/app/bootstrap.c, app/src/persistence/persistence.h, app/src/persistence/persistence.c, app/src/persistence/persistence_types.h</files>
  <action>Load the resolved persisted snapshot into `app_context`, expose centralized read/save entrypoints that later auth, relay/action, and scheduler phases can consume, and keep all store access inside the persistence module rather than sprinkling flash calls across future service code.</action>
  <verify>
    <automated>./scripts/build.sh &amp;&amp; rg -n "persisted_config|section_status|persistence_store_(load|save)" app/src/app/app_context.h app/src/app/bootstrap.c app/src/persistence/persistence.h app/src/persistence/persistence.c</automated>
  </verify>
  <done>The app boots with a real loaded persistence snapshot contract ready for downstream phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "APP_ADMIN_USERNAME|APP_ADMIN_PASSWORD|auth|reseed|credentials" app/src/persistence/persistence.h app/src/persistence/persistence.c app/src/persistence/persistence_types.h app/src/app/bootstrap.c
- [ ] rg -n "last_desired|reboot_policy|action|schedule|disabled|reference|validate" app/src/persistence/persistence.h app/src/persistence/persistence.c app/src/persistence/persistence_types.h
- [ ] rg -n "persisted_config|section_status|persistence_store_(load|save)" app/src/app/app_context.h app/src/app/bootstrap.c app/src/persistence/persistence.h app/src/persistence/persistence.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Auth, relay/action, and schedule configuration all load through typed section repositories
- Missing or invalid auth data reseeds from the configured source while clean-device automation defaults remain safe and blank
- Relay reboot policy, relay last desired state, and schedule/action reference validation all exist behind centralized persistence APIs
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistent-configuration/04-02-SUMMARY.md`
</output>
