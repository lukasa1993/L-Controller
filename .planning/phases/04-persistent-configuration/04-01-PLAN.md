---
phase: 04-persistent-configuration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/CMakeLists.txt
  - app/pm_static.yml
  - app/Kconfig
  - app/prj.conf
  - app/src/app/app_config.h
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/persistence/persistence.h
  - app/src/persistence/persistence.c
  - app/src/persistence/persistence_types.h
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
user_setup: []
must_haves:
  truths:
    - "Boot has a single persistence entrypoint that mounts the durable store from a reserved storage partition, loads a resolved configuration snapshot before downstream consumers, and keeps clean-device defaults safe."
    - "Persisted auth, action/relay, and schedule data are defined as explicit versioned sections with typed load outcomes instead of ad hoc flash records."
    - "The persistence boundary owns backend details centrally so later auth, relay/action, and scheduler work consume typed configuration contracts instead of raw storage primitives."
  artifacts:
    - path: "app/src/persistence/persistence.h"
      provides: "Public persistence-store contract with section load/save APIs and load-status reporting"
      min_lines: 60
      contains: "struct persistence_store"
    - path: "app/src/persistence/persistence_types.h"
      provides: "Versioned persisted-section types and status enums for auth, action/relay, and schedule configuration"
      min_lines: 80
      contains: "enum persistence_section"
    - path: "app/src/app/bootstrap.c"
      provides: "Boot-time persistence initialization and snapshot loading before downstream consumers"
      contains: "persistence_store_"
    - path: "app/pm_static.yml"
      provides: "Reserved `settings_storage` flash partition for durable configuration data"
      contains: "settings_storage"
    - path: "app/Kconfig"
      provides: "Explicit persistence and relay reboot-policy configuration knobs"
      contains: "APP_PERSISTENCE_"
  key_links:
    - from: "app/src/app/bootstrap.c"
      to: "app/src/persistence/persistence.h"
      via: "boot initializes the persistence store and loads the resolved snapshot before service startup"
      pattern: 'persistence_store_(init|load)'
    - from: "app/src/app/app_context.h"
      to: "app/src/persistence/persistence.h"
      via: "app context owns the persistence store and resolved configuration state by value"
      pattern: 'persistence_store|persisted_'
    - from: "app/src/persistence/persistence.c"
      to: "app/Kconfig"
      via: "store backend and safe defaults consume build-time persistence/auth/reboot-policy configuration"
      pattern: 'CONFIG_APP_'
---

<objective>
Define the storage schema, versioning rules, and persistence ownership boundary for Phase 4.

Purpose: Turn the stub persistence seam into a real boot-owned contract that can safely carry persisted auth, action/relay, and schedule data without pulling later control-panel or execution behavior forward.
Output: A reserved storage partition, a compiled persistence module, versioned section contracts, and a boot-time init/load boundary owned by `app_boot()` and `app_context`.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-persistent-configuration/04-CONTEXT.md
@.planning/phases/04-persistent-configuration/04-RESEARCH.md
@.planning/phases/04-persistent-configuration/04-VALIDATION.md
@app/CMakeLists.txt
@app/Kconfig
@app/prj.conf
@app/src/app/app_config.h
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/persistence/persistence.h
@app/src/panel/panel_auth.h
@app/src/actions/actions.h
@app/src/relay/relay.h
@app/src/scheduler/scheduler.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the persistence backend surface and build integration</name>
  <files>app/CMakeLists.txt, app/pm_static.yml, app/Kconfig, app/prj.conf, app/src/app/app_config.h, app/src/persistence/persistence.h, app/src/persistence/persistence.c, app/src/persistence/persistence_types.h</files>
  <action>Add a real persistence module to the build, reserve a dedicated `settings_storage` partition through Partition Manager, enable the required Zephyr flash-backed storage primitives, and introduce only the small build-time configuration surface this phase needs: persistence layout/version knobs if required by the chosen backend, plus the clean-device default relay reboot-policy setting. Keep Wi-Fi credentials build-time only and do not introduce runtime onboarding or panel UX.</action>
  <verify>
    <automated>test -f app/pm_static.yml &amp;&amp; rg -n "settings_storage" app/pm_static.yml &amp;&amp; test -f app/src/persistence/persistence.c &amp;&amp; rg -n "CONFIG_(SETTINGS|NVS|FLASH|FLASH_MAP)|APP_PERSISTENCE_|APP_RELAY_.*REBOOT" app/Kconfig app/prj.conf app/src/app/app_config.h</automated>
  </verify>
  <done>The repo compiles a real persistence module and declares an explicit storage plus reboot-policy configuration surface.</done>
</task>

<task type="auto">
  <name>Task 2: Define versioned section types and load/fallback reporting</name>
  <files>app/src/persistence/persistence.h, app/src/persistence/persistence.c, app/src/persistence/persistence_types.h</files>
  <action>Define the typed persisted records and section-level load outcome enums/structs for auth, action/relay, and schedule configuration. Make the load status expressive enough to distinguish loaded, empty-default, invalid-reset, incompatible-reset, and auth-reseeded outcomes so later status surfaces can explain what happened without inspecting raw storage.</action>
  <verify>
    <automated>rg -n "enum persistence_section|enum persistence_load_state|struct persisted_(auth|action|relay|schedule|config)|schema_version|reseeded" app/src/persistence/persistence.h app/src/persistence/persistence_types.h app/src/persistence/persistence.c</automated>
  </verify>
  <done>Phase 4 has explicit versioned persistence contracts and operator-meaningful section load outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire boot ownership for persistence without pulling future features forward</name>
  <files>app/src/app/app_context.h, app/src/app/bootstrap.c, app/src/persistence/persistence.h, app/src/persistence/persistence.c</files>
  <action>Extend `app_context` to own the persistence store and resolved persisted snapshot by value, initialize or mount the store in `app_boot()` after build-time config is available, and load the snapshot before downstream subsystems need it. Keep later auth, panel, relay, action, and scheduler behavior out of scope; this plan establishes only the durable contract and boot-time ownership boundary.</action>
  <verify>
    <automated>rg -n "struct persistence_store|struct persisted_config|persistence_store_(init|load)" app/src/app/app_context.h app/src/app/bootstrap.c app/src/persistence/persistence.h app/src/persistence/persistence.c</automated>
  </verify>
  <done>Boot has a single authoritative persistence init/load boundary and future consumers have a typed snapshot to read.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] test -f app/pm_static.yml && rg -n "settings_storage" app/pm_static.yml && rg -n "CONFIG_(SETTINGS|NVS|FLASH|FLASH_MAP)|APP_PERSISTENCE_|APP_RELAY_.*REBOOT" app/Kconfig app/prj.conf app/src/app/app_config.h
- [ ] rg -n "enum persistence_section|enum persistence_load_state|struct persisted_(auth|action|relay|schedule|config)|schema_version|reseeded" app/src/persistence/persistence.h app/src/persistence/persistence_types.h app/src/persistence/persistence.c
- [ ] rg -n "struct persistence_store|struct persisted_config|persistence_store_(init|load)" app/src/app/app_context.h app/src/app/bootstrap.c app/src/persistence/persistence.h app/src/persistence/persistence.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Boot owns a centralized persistence init/load seam before downstream consumers and mounts storage from an explicit reserved partition
- Persisted auth, action/relay, and schedule data have explicit versioned section contracts and load-status reporting
- Backend details stay inside the persistence boundary instead of leaking into future service modules
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistent-configuration/04-01-SUMMARY.md`
</output>
