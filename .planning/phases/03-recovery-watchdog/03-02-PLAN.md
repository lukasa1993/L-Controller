---
phase: 03-recovery-watchdog
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - README.md
  - scripts/common.sh
  - scripts/validate.sh
autonomous: false
requirements: [REC-01, REC-02]
user_setup: []
must_haves:
  truths:
    - "The repo keeps a single automated validation entrypoint for fast build feedback while clearly separating device-only recovery/watchdog sign-off."
    - "Phase 3 validation explicitly covers healthy stability, long-degraded escalation, preserved reset-cause reporting, watchdog reset semantics, and anti-thrash behavior."
    - "Final phase approval depends on a human-confirmed device run that records the observed recovery-reset and watchdog behavior."
  artifacts:
    - path: "README.md"
      provides: "Documented Phase 3 validation flow and expected recovery/watchdog scenarios"
      contains: "watchdog"
    - path: "scripts/validate.sh"
      provides: "Phase-safe automated validation entrypoint"
      min_lines: 20
      contains: "build.sh"
    - path: "scripts/common.sh"
      provides: "Shared validation marker and device-checklist helpers for recovery sign-off"
      contains: "marker"
  key_links:
    - from: "scripts/validate.sh"
      to: "scripts/build.sh"
      via: "delegated automated build validation"
      pattern: 'build\.sh'
    - from: "README.md"
      to: "scripts/flash.sh"
      via: "documented device validation sequence"
      pattern: 'flash\.sh|console\.sh'
---

<objective>
Validate stuck-state detection, escalation thresholds, and reset semantics.

Purpose: Preserve fast automated feedback while making the blocking device-level recovery and watchdog scenarios explicit, repeatable, and phase-complete.
Output: Updated validation guidance, a phase-safe automated path, and a recorded human checkpoint covering healthy stability, long-degraded escalation, recovery-reset breadcrumbs, watchdog resets, and anti-thrash behavior.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
@/Users/l/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-recovery-watchdog/03-CONTEXT.md
@.planning/phases/03-recovery-watchdog/03-RESEARCH.md
@.planning/phases/03-recovery-watchdog/03-VALIDATION.md
@README.md
@scripts/build.sh
@scripts/common.sh
@scripts/doctor.sh
@scripts/flash.sh
@scripts/console.sh
@scripts/validate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update validation guidance for recovery/watchdog markers and scenarios</name>
  <files>README.md, scripts/common.sh, scripts/validate.sh</files>
  <action>Update the repo documentation and validation messaging so Phase 3 clearly distinguishes automated build validation from device-only recovery verification. Document the expected healthy, long-degraded, reboot-breadcrumb, watchdog-reset, and anti-thrash scenarios, and keep the automated path safe for frequent iteration.</action>
  <verify>
    <automated>bash -n scripts/validate.sh scripts/common.sh &amp;&amp; rg -n "watchdog|recovery|reset cause|validate\.sh|flash\.sh|console\.sh" README.md scripts/common.sh scripts/validate.sh</automated>
  </verify>
  <done>The repo explains how to validate the recovery/watchdog behavior while preserving a single fast automated entrypoint for normal development loops.</done>
</task>

<task type="auto">
  <name>Task 2: Stage the device checklist and preserve the automated build path</name>
  <files>README.md, scripts/validate.sh, scripts/common.sh</files>
  <action>Ensure the documented device checklist covers patience-window escalation, preserved reset-cause visibility, watchdog-reset confirmation, and anti-thrash behavior instead of only a happy-path reboot. Keep `./scripts/validate.sh` positioned as the canonical wave-level automated validation command, and make the hardware gate explicitly blocking for phase approval.</action>
  <verify>
    <automated>bash -n scripts/validate.sh scripts/common.sh &amp;&amp; rg -n "build\.sh|stable|degraded|reset|watchdog" README.md scripts/validate.sh scripts/common.sh</automated>
  </verify>
  <done>The automated validation path remains intact and the device scenario checklist is ready for the blocking final checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm recovery and watchdog behavior on real hardware</name>
  <files>README.md, scripts/flash.sh, scripts/console.sh</files>
  <action>Collect final real-device confirmation that the recovery manager and watchdog behavior are correct across healthy, degraded, recovery-reset, and anti-thrash scenarios before Phase 3 can be considered complete.</action>
  <verify>Human checkpoint approved with the required healthy, long-degraded, reboot-breadcrumb, watchdog-reset, and anti-thrash scenarios observed, or the failing scenario is recorded.</verify>
  <done>Phase 3 hardware validation is explicitly approved or blocked with a concrete scenario-level note.</done>
  <what-built>Phase 3 recovery/watchdog validation flow and documented device checklist</what-built>
  <how-to-verify>
    Using the built firmware and the repo scripts, confirm the recovery manager behaves correctly on real hardware:
    1. Flash the image with `./scripts/flash.sh`
    2. Open the device console with `./scripts/console.sh`
    3. Verify a normal boot reaches `APP_READY`, enters the healthy path, and does not trigger a false recovery reset during the stable run window
    4. Force a long-enough degraded or stalled network path and confirm ordinary retries happen before escalation instead of every transient immediately resetting the device
    5. Confirm the eventual escalation produces a whole-device reset and the next boot logs the retained recovery-reset cause clearly
    6. Use the selected lab workflow for the phase implementation to confirm a true watchdog-feed starvation path resets the device under watchdog control
    7. Recreate or maintain the fault after reboot and confirm the system does not fall into a rapid reset loop
    8. Record which healthy, long-degraded, reboot-breadcrumb, watchdog-reset, and anti-thrash scenarios passed or failed in the Phase 3 summary
  </how-to-verify>
  <resume-signal>Type "approved" once the Phase 3 device scenarios are confirmed, or describe the failing scenario</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/validate.sh
- [ ] bash -n scripts/validate.sh scripts/common.sh
- [ ] rg -n "watchdog|recovery|reset cause|flash\.sh|console\.sh" README.md scripts/common.sh scripts/validate.sh
- [ ] Human checkpoint approved with healthy, long-degraded, reboot-breadcrumb, watchdog-reset, and anti-thrash scenarios recorded
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo clearly separates automated build validation from device-only recovery/watchdog sign-off
- Phase 3 validation covers healthy stability, patience-window escalation, watchdog reset semantics, retained reset-cause reporting, and anti-thrash behavior explicitly
- Final approval records the real-device result for each required recovery/watchdog scenario

</success_criteria>

<output>
After completion, create `.planning/phases/03-recovery-watchdog/03-02-SUMMARY.md`
</output>
