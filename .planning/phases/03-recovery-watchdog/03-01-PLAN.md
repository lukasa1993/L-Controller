---
phase: 03-recovery-watchdog
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/CMakeLists.txt
  - app/Kconfig
  - app/prj.conf
  - app/src/app/app_config.h
  - app/src/app/app_context.h
  - app/src/app/bootstrap.c
  - app/src/network/network_state.h
  - app/src/network/network_supervisor.c
  - app/src/network/network_supervisor.h
  - app/src/recovery/recovery.h
  - app/src/recovery/recovery.c
autonomous: true
requirements: [REC-01, REC-02]
user_setup: []
must_haves:
  truths:
    - "Critical execution paths report progress into a dedicated recovery manager, and the watchdog is only fed when those paths remain within allowed progress windows."
    - "Confirmed long-lived degraded or stalled network supervision is treated as unrecoverable only after explicit patience thresholds, not on every transient Wi-Fi failure."
    - "The device preserves the most recent recovery-reset cause across reboot and avoids immediate reset thrashing before returning to normal supervision."
  artifacts:
    - path: "app/src/recovery/recovery.c"
      provides: "Recovery manager implementation that owns watchdog setup, supervised-path liveness windows, and retained reset breadcrumb handling"
      min_lines: 140
      contains: "wdt_"
    - path: "app/src/recovery/recovery.h"
      provides: "Recovery manager contract for initialization, progress reporting, and retained reset-cause access"
      min_lines: 30
      contains: "recovery_manager"
    - path: "app/Kconfig"
      provides: "Explicit recovery/watchdog timing configuration"
      contains: "APP_RECOVERY_"
    - path: "app/prj.conf"
      provides: "Zephyr features required for watchdog and reset-cause support"
      contains: "CONFIG_WATCHDOG"
  key_links:
    - from: "app/src/app/bootstrap.c"
      to: "app/src/recovery/recovery.h"
      via: "boot initializes recovery before starting long-lived supervision and logs preserved reset cause"
      pattern: 'recovery_manager_'
    - from: "app/src/network/network_supervisor.c"
      to: "app/src/recovery/recovery.h"
      via: "network supervision reports forward progress and degraded incidents into recovery policy"
      pattern: 'recovery_manager_|progress|degraded'
    - from: "app/src/recovery/recovery.c"
      to: "app/src/network/network_state.h"
      via: "recovery policy reads shared runtime state and classifies stuck versus progressing network conditions"
      pattern: 'network_runtime_state|last_failure'
---

<objective>
Implement task-level supervision and escalation policy boundaries for recovery and watchdog control.

Purpose: Turn the placeholder recovery boundary into a real recovery manager that supervises boot and network progress, arms the hardware watchdog, and escalates only after confirmed loss of progress.
Output: A recovery module, recovery/watchdog config, network integration, and retained reset breadcrumb handling that keeps ordinary network churn inside the existing Phase 2 self-healing loop.
</objective>

<execution_context>
@/Users/l/.codex/get-shit-done/workflows/execute-plan.md
@/Users/l/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-recovery-watchdog/03-CONTEXT.md
@.planning/phases/03-recovery-watchdog/03-RESEARCH.md
@.planning/phases/03-recovery-watchdog/03-VALIDATION.md
@app/CMakeLists.txt
@app/Kconfig
@app/prj.conf
@app/src/app/app_config.h
@app/src/app/app_context.h
@app/src/app/bootstrap.c
@app/src/network/network_state.h
@app/src/network/network_supervisor.c
@app/src/network/network_supervisor.h
@app/src/recovery/recovery.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the recovery manager contract and build-time watchdog policy</name>
  <files>app/CMakeLists.txt, app/Kconfig, app/prj.conf, app/src/app/app_config.h, app/src/recovery/recovery.h, app/src/recovery/recovery.c</files>
  <action>Add a dedicated recovery manager implementation, extend the build-time app configuration with recovery/watchdog policy knobs, and enable the Zephyr watchdog / reset-cause features needed for this phase. Keep the policy surface intentionally small: watchdog timeout, degraded patience, stable-run clearing, and anti-thrash timing only.</action>
  <verify>
    <automated>test -f app/src/recovery/recovery.c &amp;&amp; rg -n "APP_RECOVERY_|CONFIG_WATCHDOG|CONFIG_HWINFO" app/Kconfig app/prj.conf &amp;&amp; rg -n "struct app_recovery_config|recovery_manager" app/src/app/app_config.h app/src/recovery/recovery.h app/src/recovery/recovery.c</automated>
  </verify>
  <done>The repo has a real recovery manager boundary plus explicit build-time policy knobs for watchdog and recovery timing.</done>
</task>

<task type="auto">
  <name>Task 2: Wire supervised progress and escalation boundaries through boot and network supervision</name>
  <files>app/src/app/app_context.h, app/src/app/bootstrap.c, app/src/network/network_state.h, app/src/network/network_supervisor.c, app/src/network/network_supervisor.h, app/src/recovery/recovery.c</files>
  <action>Extend the app context to own the recovery manager, initialize it during boot, and have the network supervisor report meaningful progress and degraded-state transitions into recovery policy. Keep ordinary connect, DHCP, reachability, and reconnect behavior inside the network supervisor; reboot and watchdog policy must live behind the recovery manager boundary instead of leaking into network code.</action>
  <verify>
    <automated>rg -n "recovery_manager_" app/src/app/bootstrap.c app/src/network/network_supervisor.c app/src/recovery/recovery.c &amp;&amp; rg -n "startup_outcome_sem|last_failure|connectivity_state" app/src/network/network_state.h app/src/network/network_supervisor.c &amp;&amp; ! rg -n "sys_reboot|wdt_" app/src/network/network_supervisor.c</automated>
  </verify>
  <done>Boot and network supervision feed recovery policy with real progress signals while keeping device-reset ownership out of the network layer.</done>
</task>

<task type="auto">
  <name>Task 3: Add retained reset-cause handling and anti-thrash stable-window behavior</name>
  <files>app/src/recovery/recovery.c, app/src/recovery/recovery.h, app/src/app/bootstrap.c</files>
  <action>Preserve the most recent recovery-triggered reset cause across reboot using retained state, surface that cause during the next boot, and ensure the device does not immediately re-escalate into another reset without first respecting the configured cooldown or stable-run rules. Keep the retained incident model deliberately minimal rather than turning it into a general persistence feature.</action>
  <verify>
    <automated>rg -n "wdt_(install_timeout|setup|feed)|hwinfo_get_reset_cause|__noinit|stable|cooldown|reset cause" app/src/recovery/recovery.c app/src/recovery/recovery.h app/src/app/bootstrap.c</automated>
  </verify>
  <done>The firmware can explain the last recovery reset, classify anti-thrash timing consistently, and escalate only through the dedicated recovery path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ./scripts/build.sh
- [ ] rg -n "APP_RECOVERY_|CONFIG_WATCHDOG|CONFIG_HWINFO" app/Kconfig app/prj.conf
- [ ] rg -n "recovery_manager_" app/src/app/bootstrap.c app/src/network/network_supervisor.c app/src/recovery/recovery.c
- [ ] rg -n "wdt_(install_timeout|setup|feed)|hwinfo_get_reset_cause|__noinit" app/src/recovery/recovery.c app/src/recovery/recovery.h
- [ ] ! rg -n "sys_reboot|wdt_" app/src/network/network_supervisor.c
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Recovery policy supervises real progress boundaries instead of a blind watchdog feed loop
- Ordinary Wi-Fi churn stays inside Phase 2 self-healing unless a separate Phase 3 patience window is exceeded
- The next boot can report the most recent recovery-triggered reset cause without requiring Phase 4 persistence

</success_criteria>

<output>
After completion, create `.planning/phases/03-recovery-watchdog/03-01-SUMMARY.md`
</output>
